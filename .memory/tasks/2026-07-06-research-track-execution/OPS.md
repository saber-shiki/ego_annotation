# OPS — append-only

## 2026-07-06 — Task initialized
Created research-track execution task from mechanism-driven planning artifact. Canonical task pack copied to `TASK_PACK.md`. Initial execution target is R0/R1: `ego.hoi` sidecar substrate and graph instrumentation for support, liveness, gauge, freshness, stale dependencies, input-output deltas, and render-state lineage. Actual implementation work will start on branch `yiwen_research` after the task scaffolding is committed and pushed to main.

## 2026-07-06 — Research branch started
Switched to branch `yiwen_research` from pushed mainline commit `c7d77c3`. Launched async subagent run `05de5157-dd9e-4f0e-8951-cc6c42b6536d` with three concrete R0/R1 workstreams: graph/render inventory, first-slice causal-card selection, and minimal ego.hoi graph-health scaffold implementation. All tasks are constrained to mechanism-driven outputs; implementation task may create new files only and must avoid dirty existing files.

## 2026-07-06 — First slice selected and scaffold router extended
Subagent run `05de5157-dd9e-4f0e-8951-cc6c42b6536d` completed. Selected first R0/R1 slice: HOT3D clip001850 keyboard, right hand, frames ~26-46, run `/data2/ego_annotation_outputs/v19_runs/20260626_hot3d_clip001850_pinhole_a800_native_v5_focalfix_coordrigid_v1`. Observed contradiction: interval MANO reports ~10 cm penetration into observed keyboard surface; contact/nonpenetration reports zero penetrating vertices against a different mostly-TRELLIS non-watertight completed mesh with zero signed-query candidates; published render reports +39 mm gap; rigid pose graph is inert (`nfev=1`, cost 0, deltas 0, nonpenetration target frames 0). Updated EPISTEMIC/PROMPT/TASK_PACK and extended `scripts/build_ego_hoi_sidecar_and_graph_health.py` with `cross_solver_geometry_decoupled` route and synthetic `geometry_mismatch` mode. Validation: synthetic modes `none/support/inert/geometry_mismatch/stale_render/stale_join` route to distinct decisions; `geometry_mismatch` asserts geometry source mismatch, zero signed candidates, and mismatch reasons.

## 2026-07-06 — Clip001850 reconciliation work dispatched
Launched async run `c1ccadb3-e2bd-4152-9917-6612623b9bd7` on branch `yiwen_research` for the next artifact-changing step. Contracts: (1) implement and run CPU-only cross-source contact/geometry ledger for HOT3D clip001850 keyboard right hand frames 26-46; (2) build adapter that turns pose graph + interval MANO + contact/NP + geometry + render reports into a graph summary consumed by `build_ego_hoi_sidecar_and_graph_health.py`, expecting `cross_solver_geometry_decoupled`; (3) attack the reconciliation with artifact-level kill-tests. Expected artifact is not a report: per-frame ego.hoi-style contact_frame_detail/geometry provenance rows plus a graph_health row for the real slice.

## 2026-07-06 — Reconciliation result changed route
Subagent run `c1ccadb3-e2bd-4152-9917-6612623b9bd7` produced a per-frame cross-source ledger and real-slice graph summary, but the critique changed the causal route. Key new evidence: interval dense observed-surface penetration comes from full-frame UniDepth with hand-owned quarantine off, object-mask gate off, surface eligibility off, and zero selected depth-order-vs-object vertices; the channel may be table/background/hand-depth leakage. Completed-mesh contact/NP remains disabled by a 95.4%-TRELLIS non-watertight mesh with free space not evaluated. Published rows are not consumed by a renderer. Updated EPISTEMIC: do not wire observed-surface penetration into contact/NP until KT-1/KT-2/KT-3 survive; route uncertain frames to explicit contact_state/provenance and make that state visible.

## 2026-07-06 — Kill-test implementation dispatched
Launched async run `42def61e-14d9-4199-8918-3064df1a39dd` for the next artifact-changing work: keyboard-masked/hand-quarantined depth re-query and per-vertex coherence (KT-1/KT-2/KT-3/KT-5), provenance-hash graph-summary patch (KT-6), ego.hoi-consuming f32/f36 visual artifact (KT-7), and implementable contact-state policy.

## 2026-07-06 — Clip001850 kill-tests overturned wire-in route
Subagent run `42def61e-14d9-4199-8918-3064df1a39dd` completed. KT-1/2/3/5 outputs under `/tmp/clip001850_masked_contact_killtests/` show keyboard-masked + hand-quarantined depth refutes interval mesh penetration on mask-available frames. f32: interval mesh penetration max 0.08196 m while keyboard-HQ median delta is -0.14428 m with 0 eligible penetrating vertices. f36: interval mesh penetration max 0.10678 m while keyboard-HQ median delta is -0.08085 m with 0 eligible penetrating vertices; full-frame leak is only max +0.02159 m on non-keyboard pixels. Route distribution over f28-48: pose_unresolved 12, geometry_epoch_contaminated 7, full_frame_depth_leak 1, contact_candidate_keyboard_masked 1. Mechanism changes from “wire observed-surface penetration” to M2 geometry epoch contamination plus likely hand-depth bias. KT-6 patch made graph-health provenance hash-derived and incomplete-provenance routed to evidence_incomplete. Updated `scripts/render_clip001850_egohoi_contact_review.py` to consume the kill-test rows; `/tmp/clip001850_egohoi_contact_review/egohoi_review_f032.jpg` now renders `geometry_epoch_contaminated`, and `egohoi_review_f036.jpg` renders `full_frame_depth_leak`.

## 2026-07-06 — Durable clip001850 contact-state artifact copied
Copied the post-kill-test artifact to `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/`. Key files: `masked_contact_killtests/contact_killtests_frame_detail.ndjson`, `masked_contact_killtests/summary.json`, `egohoi_contact_review/egohoi_review_f032.jpg`, `egohoi_contact_review/egohoi_review_f036.jpg`, `egohoi_graph_health_provenance/ego_hoi/graph_health.json`, and `artifact_manifest.json`. The review images are driven by kill-test route states: f32 `geometry_epoch_contaminated`, f36 `full_frame_depth_leak`.

## 2026-07-06 — Next root mechanisms dispatched
Launched async run `a274acba-4071-403a-be8b-9f6b3d564bea` for the next artifact-changing split after KT-1/2/3/5. Branches: (1) observed/free-space keyboard contact-body repair on direct-pose mask-available frames; (2) hand-depth-bias counterfactual to quantify the along-ray shift needed for keyboard contact; (3) canonical `ego.hoi contact_frame_detail` writer from kill-test policy; (4) artifact-level critique of geometry-repair and hand-depth-repair branches. Current prior: geometry epoch contamination dominates the interval penetration, but an ~8 cm hand-depth bias may explain why masked keyboard depth shows the hand in front of the keyboard.

## 2026-07-06 — Geometry and hand-depth repair branches closed for clip001850
Subagent run `a274acba-4071-403a-be8b-9f6b3d564bea` completed and results were integrated. Object-body repair extracted a 3221-face observed-only keyboard body and carved 49,222 of 87,671 carveable TRELLIS/unsupported faces, but observed-only hand distances remain 35.6-115.2 mm and the body remains non-watertight; geometry repair invalidates phantom penetration but does not create contact. Hand-depth counterfactual requires 83-202 mm along-ray shifts (median 134 mm), causes 62-169 px median reprojection displacement, and creates a candidate only on f36; contact-forcing hand-depth correction is rejected. Canonical contact table demotes f45 from `contact_candidate_keyboard_masked` to `unresolved_incoherent_evidence` because it is one thumb vertex on a near-depth outlier with no persistent patch. Durable artifact refreshed at `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/`; canonical counts are pose_unresolved 12, geometry_epoch_contaminated 7, full_frame_depth_leak 1, unresolved_incoherent_evidence 1.

## 2026-07-06 — Production-style render consumer dispatched
Launched async run `332a242f-0d64-47e5-8a9e-4377dfc7ad63` to make the canonical `contact_frame_detail` table affect a v19-style rendered artifact. Required visible states: f32 `geometry_epoch_contaminated`, f36 `full_frame_depth_leak`, f45 `unresolved_incoherent_evidence`; object body must be uncertain/observed/repaired rather than solid accepted TRELLIS; published gap/penverts/UNCERTAIN must be suppressed or replaced by canonical contact_state labels.

## 2026-07-06 — Full-duration contact-state render consumer completed
Subagent run `332a242f-0d64-47e5-8a9e-4377dfc7ad63` returned three results. Subagent 15 produced useful body/label drawing code but only three-frame stills from `/tmp`, so it did not close the production consumer gap. Subagent 17 identified the actual render mechanism: clip001850's published videos are Stage-1 `render_v18_compact_rigid_tomato_temporal_mano_attempt.py` with raw TRELLIS body/`penverts=0`/bare `UNCERTAIN`, then Stage-2 `publish_v19_render_artifact.py` with one static banner; Stage-2 cannot carry per-frame contact states. Implemented `scripts/render_clip001850_v19_contact_state_full_duration.py` as the artifact-changing replacement: it reads durable `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/contact_state_table/contact_frame_detail.ndjson` and durable `keyboard_body_repair/repaired_observed_contact_body.ply`, renders all 150 frames at 30 fps, defaults 129 uncovered frames to `unresolved_evidence_incomplete`, and writes `/tmp/clip001850_v19_contact_state_full_duration/{v19_overlay.mp4,v19_world.mp4,v19_side_by_side.mp4,manifest.json}`. Validation: contact-state counts are `unresolved_evidence_incomplete=129`, `pose_unresolved=12`, `geometry_epoch_contaminated=7`, `full_frame_depth_leak=1`, `unresolved_incoherent_evidence=1`; f32/f36/f45 states are contaminated/leak/incoherent; observed body hash prefix `6d4bcebfb76149a4`, 3221 faces; no `gap 39.2mm`, `penverts=0`, bare `UNCERTAIN`, or `confirmed_contact` appears in replacement text; output videos are 150 frames with dimensions overlay 960x1038, world 1280x798, side-by-side 1920x618. Durable copy refreshed at `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/v19_contact_state_full_duration/` and artifact_manifest.json updated.

## 2026-07-06 — Next research mechanisms dispatched after full-duration contact render
Launched async run `0d307e4f-43cd-47b5-a98a-2bb5dc63f90d` with four workstreams: (18) promote the accepted clip001850 full-duration contact-state render into a reusable parameterized runtime-style consumer; (19) test floor-independent motion-coupling contact adjudication for clip001850 without signed-distance promotion; (20) package current slice into an `ego.hoi` sidecar that points to the full-duration render and includes contact/geometry/render provenance; (21) rank the next research mechanisms with causal cards. Existing still-only consumer from subagent 15 remains QC evidence only; accepted render artifact is the full-duration `/data2/.../v19_contact_state_full_duration/` output.

## 2026-07-06 — Generic render, motion null, sidecar, and next mechanism integrated
Async run `0d307e4f-43cd-47b5-a98a-2bb5dc63f90d` completed. Subagent 18 produced generic additive `scripts/render_v19_contact_state_full_duration.py`, validated by rerendering clip001850 to `/tmp/clip001850_v19_contact_state_generic_render/`; all generic acceptance gates pass and the rendered body hash matches observed body `6d4bcebfb76149a4...`. Subagent 19 produced additive `scripts/analyze_clip001850_motion_coupling_contact.py`; verdict `motion_coupling_absent_no_impulse_above_motion_floor`, `promoted_frames=[]`, direct-edge motion floor 76.9 mm / 18.2 deg, f36 approach response 8.6 mm below window median, Spearman rho -0.25 p 0.59, f46 dominant edge is withdrawal-time refit. Subagent 20 produced additive `scripts/build_clip001850_ego_hoi_sidecar_package.py`; sidecar reproduces f32/f36/f45 from table and points to full-duration render hashes. Subagent 21 ranked next mechanism: graph-factor liveness/object-pose trajectory first; HOT3D GT/R8 preflight second; motion coupling on a moving-object clip after trajectory liveness third. Copied generic render output, motion-coupling output, and ego.hoi sidecar into `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/` and refreshed `artifact_manifest.json` (83 files tracked in manifest).

## 2026-07-06 — Pose graph liveness mechanism dispatched
Launched async run `1135fef1-61ea-4969-89a9-81560f70a439` for the next root blocker after contact-state resolution. Workstreams: (22) source-of-truth inventory of the clip001850 keyboard pose graph and solver/objective path; (23) additive CPU liveness probe measuring pose status counts, support, frozen clusters, jumps, residual/optimizer evidence, and dominant mechanism; (24) pose-liveness render review exposing measured/held/interpolated/ineligible body placement and jumps; (25) adversarial acceptance criteria for a real liveness intervention. The discriminating mechanisms are no seed/correspondence support, visible-surface degeneracy, solver plumbing inertness, and legitimate static-held gauge.

## 2026-07-06 — Pose liveness evidence integrated and intervention frame corrected
Async run `1135fef1-61ea-4969-89a9-81560f70a439` completed. Subagent 22 showed P15 inertness is by construction: the solver optimizes correction deltas, P15 ran without `--constraint-report`, P16 has zero accepted nonpenetration correction rows, and all remaining residuals are homogeneous at zero correction. Subagent 23 produced `scripts/analyze_clip001850_pose_graph_liveness.py` and `/tmp/clip001850_pose_graph_liveness/`: pose statuses are missing 65 / ineligible 72 / direct fit 13; support is depth-ICP only on 13 frames, temporal/nonpenetration inactive; graph is pass-through with zero input-output delta; direct edges jump up to 166.4 mm / 42.1 deg; nearest-hold collapses to two poses (f30 head hold and f77 tail hold). Subagent 24 produced `scripts/render_clip001850_pose_liveness_review.py` and `/tmp/clip001850_pose_liveness_review/`, making frozen/interpolated/direct/jump provenance visible with no contact claim. Subagent 25 attacked smoothing: the keyboard is static/rest-cluster plus outliers, the pre-existing smoother fabricates a slide, adds zero support, and leaves f78-149 near the f77 outlier. Resolved a numeric apparent contradiction: ineligible visible-geometry diagonal extent ratio median/max is 1.66/2.43, while worst-axis ratio median/max is 4.72/7.18; both describe the same anisotropic SAM2 mask leakage. Copied liveness outputs to `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/{pose_graph_liveness,pose_liveness_review}/` and refreshed `artifact_manifest.json` (91 files tracked). Next intervention is KT-L1/KT-L2/KT-L5 regime classification/support recovery/static gauge, not trajectory smoothing.

## 2026-07-06 — KT-L1/KT-L2 regime and support recovery dispatched
Launched async run `20e2ea18-fb69-4779-9f77-2ffe66d6f840` for the next artifact-changing decision after pose liveness. Workstreams: (26) KT-L1 pose-independent regime classification (static-observed vs moving-observed vs mask-drift vs occluded/out-of-view), using observed-surface centroids, mask consistency, reprojection/crop evidence, and rest-cluster consensus; (27) KT-L2 mask/support recovery on the 72 visible-surface-ineligible frames using existing masks/depth/MANO/anchor evidence only, with no new model inference and no smoothing. The explicit stop condition is a route decision: robust static-held gauge with visibility ledger, recovered support raising direct coverage, moving trajectory only if independently observed, or unresolved/unrecoverable mask drift.

## 2026-07-06 — KT-L1/KT-L2 closed route to static-held gauge
Async run `20e2ea18-fb69-4779-9f77-2ffe66d6f840` completed. Subagent 26 produced `scripts/analyze_clip001850_pose_regime_kt_l1.py` and `/tmp/clip001850_pose_regime_kt_l1/`: route verdict `static-held gauge`; regime counts static_observed=8 (f30-36,46), unresolved=1 (f45), mask_drift=76 (f60,f75,f76,f77,f78-149), occluded_out_of_view=65; moving_observed=0. Critical mechanism: the visible-geometry anchor f75 is itself mask drift, 199.5 mm from the rest cluster `[0.599,-0.094,0.339]`, so the upstream extent gate and ICP seed are contaminated. Subagent 27 produced `scripts/probe_clip001850_mask_recovery_kt_l2.py` and `/tmp/clip001850_mask_recovery_kt_l2/`: all 72 f78-149 ineligible frames are `mask_drift_delaminated`; hand+depth-cleaned centroids remain 138-278 mm from rest (median 166 mm); 0/72 recover consensus support; direct support stays 13/150. Copied outputs to `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/{pose_regime_kt_l1,mask_recovery_kt_l2}/` and refreshed `artifact_manifest.json` (97 files). Next artifact-changing route: static `T_rest` + covariance/visibility ledger + render-consumed pose rows/styling; no smoothing, no mask-quarantine recovery, no measured localization on no-surface or delaminated frames.

## 2026-07-06 — Static-gauge pose render repair dispatched
Launched async run `cfbcdc6b-63ad-48b8-ba81-4dcc22537213` for the artifact-changing route licensed by KT-L1/KT-L2. Executor contract: build static `T_rest` pose rows from f30-36,f46, visibility ledger with static_observed/unresolved_hand_occluded/rejected_mask_drift/mask_unreliable/occluded_out_of_view states, graph-health/static-gauge summary with support/covariance/outlier/source lineage, and a full-duration render where f78-149 no longer use the f77 outlier as an opaque held pose and contact labels do not change. Critic contract: reject row-only or banner-only changes, check f75 anchor exclusion, f45 handling, contact-state preservation, and visible body-placement/styling change with render lineage.

## 2026-07-06 — Static-fill pose state rejected; unknown-preserving pose artifact accepted
User challenged the static-gauge fill as a harmful prior. Mechanism correction: `T_rest` is a rest-cluster reference for outlier rejection only; writing it into `translation_world_m` on frames without observation launders a diagnostic statistic into pose state because consumers read numeric R/t regardless of flags. The static-gauge render under `static_gauge_pose_render/` is retained only as superseded negative evidence.

Accepted corrected artifact: `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/unknown_preserving_pose_render/`. Validation: `object_pose_observations.ndjson` has numeric R/t only on f30-36,f46; 142 rows have null pose and `is_measured=is_localized=false`. `visibility_ledger.ndjson` counts: `static_observed=8`, `unresolved_hand_occluded=1`, `rejected_mask_drift=4`, `mask_unreliable=72`, `occluded_out_of_view=65`. Videos `v19_overlay.mp4`, `v19_world.mp4`, `v19_side_by_side.mp4` are 150 frames at 30 fps and draw the keyboard body only on measured pose frames. Contact table hash remains `de7ebc27de68ab0b...`; f32/f36/f45 states unchanged. Durable top-level manifest and ego.hoi package were updated to mark unknown-preserving observations as current and static-fill as superseded/rejected prior-laundering evidence.

## 2026-07-06 — Package surface cleanup and pose purity audit integration
Fresh-context critic found the unknown-preserving pose artifact was initially additive rather than substitutive: the sidecar primary render still pointed to `v19_contact_state_full_duration` (P15 pose graph body on unknown frames), static-gauge pose rows remained in `tables/`, an orphan `static_pose_hypothesis.json` remained in the current pose directory, `static_gauge_summary.json` remained in the sidecar root, and graph-health render lineage was still a null stub.

Intervention: moved `object_pose_static_gauge.ndjson`, `object_visibility_ledger.ndjson`, and `static_gauge_summary.json` out of consumable package paths into `superseded_prior_laundering/`; deleted the orphan `static_pose_hypothesis.json`; repointed sidecar/top-level `render_artifacts` to `unknown_preserving_pose_render/v19_{overlay,world,side_by_side}.mp4`; demoted the P15-posed contact render to `superseded_render_artifacts`; fixed `render_consumption` row count and contact table hash split (`contact_frame_detail.ndjson` shipped full-duration table vs `contact_frame_detail_source_window.ndjson` source kill-test table); ran `scripts/audit_clip001850_pose_provenance_purity.py`; appended its graph-health row; replaced the null `render_state_hash_lineage` with the current object-pose-observation/render-state hashes.

Validation: package-surface gates passed. Primary render paths contain `/unknown_preserving_pose_render/`; sidecar `tables/` no longer contains static-gauge pose/visibility rows; no `*static_pose_hypothesis*` file remains under the research root; `static_gauge_summary.json` is absent from the sidecar root; `graph_health.ndjson` has non-null render lineage and a `pose_provenance_purity` row; declared row counts match files; `contact_frame_detail.ndjson` hash in package matches shipped 150-row table and the 21-row source table is separately hashed. Current pose table has numeric R/t only on f30-36,f46. Audit decision: `pose_channel_pure_current__original_laundering_detected`.

## 2026-07-06 — Full render-surface audit and reusable pose-state contract
Critic review found the seven sidecar/package-surface blockers were fixed, but the root `artifact_manifest.json` still advertised two P15-posed render blocks (`full_duration_render`, `generic_contact_state_render`) with green acceptance gates. Both consumed the 150-row P15 pose graph and drew keyboard body on unknown frames. Mechanism: the audit checked the current `render_artifacts` pointer only, not every root-advertised render surface.

Prediction before intervention: extending `scripts/audit_clip001850_pose_provenance_purity.py` to enumerate root/sidecar render manifests and count body draw on non-`observed_measured` frames should fail before demotion on exactly the two P15 root render blocks, while preserving the current pose-table purity result.

Observation: pre-retirement run wrote `pose_provenance_purity_audit/pre_retire_render_surface_summary.json`; `all_checks_pass=false`, decision `POSE_CHANNEL_OR_RENDER_SURFACE_PURITY_VIOLATION`, current surface violations = 2. Violations: `root.full_duration_render` and `root.generic_contact_state_render`, each `body_drawn_on_non_observed_measured_frames,consumed_pose_source_not_current_object_pose_observations`, with 142 non-measured body-drawn frames.

Intervention: moved `v19_contact_state_full_duration/`, `v19_contact_state_generic_render/`, and `static_gauge_pose_render/` under `superseded_prior_laundering/render_dirs/`; removed root `full_duration_render` and `generic_contact_state_render` top-level blocks; added explicit superseded artifact entries and `superseded_render_blocks_removed` notes; changed `render_consumption.ndjson` rows 0-2 to `superseded_rejected_pose_laundering`, rows 3-5 remain `superseded_rejected_prior_laundering`, rows 6-8 are current unknown-preserving, and rows 9-11 explicitly record the generic P15 render as `superseded_rejected_pose_laundering`. Added generic render to sidecar `superseded_render_artifacts`.

Post-intervention validation: final audit has `all_checks_pass=true`, decision `pose_channel_pure_current__original_laundering_detected`, `package_render_surface_pure=true`, current surface violations = 0, superseded bad surfaces = 3. `scripts/validate_ego_hoi_pose_state_contract.py` passes 7/7 on the sidecar package (`pose_state_contract_satisfied`). Full package gates pass: no root `full_duration_render`/`generic_contact_state_render`; no root P15/static render dirs; primary render paths are `unknown_preserving_pose_render`; render-consumption has 12 rows with explicit current/superseded statuses; graph-health has 4 rows including pose provenance, package render-surface purity, and schema contract; contact states unchanged (`f32=geometry_epoch_contaminated`, `f36=full_frame_depth_leak`, `f45=unresolved_incoherent_evidence`, zero confirmed contact).

## 2026-07-06T12:09:25+08:00 — R0 build hook accepted and clip001851 R2 preflight written
Integrated usable findings from completed subagent reports 36-38. Accepted: clip001851 is the next R2 slice and clip001849 is the control; R0 pose-state contract should be reusable after full-package builds. Rejected from report 36: any GT-aided runtime/probe path that lifts prediction tracks with GT depth/pose, because GT belongs only to post-freeze evaluation. R0 pose-state contract is now reusable via additive `scripts/run_ego_hoi_pose_state_contract_on_build.py`. Validation command: `python3 scripts/run_ego_hoi_pose_state_contract_on_build.py --package-root /data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/ego_hoi_sidecar/extensions/org.ego.hoi/0.1.0/clip001850_research`. Observation: decision `pose_state_contract_satisfied`, 7/7 checks pass, summary sha256 `82ab13ed272d04aceee7a554ecb4733bb4a4306f23e313dd49dba2fcc4f1734f`; `tables/graph_health.ndjson` hash remained `edfd444cb3e4cc91058a6827f38daf119e3057517009b382a2ebffbd69c1d5ca` and line count remained 4 before/after, confirming the wrapper logs outside declared tables.

Prediction before preflight: if clip001851 is the correct next R2 slice, local raw video/GT inputs should verify a live object-trajectory signal and persistent visibility, while the generated run contract should keep GT out of prediction and reserve it for post-freeze scoring. Command: `python3 scripts/prepare_hot3d_r2_correspondence_preflight.py`. Output root: `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/preflight/` containing `preflight_summary.json`, `causal_card.md`, and `a800_r2_run_contract.md`. Observation: clip001851 raw manifest/video, HOT3D GT sidecar, and HaWoR hands all exist locally; frame count 150, 1408x1408, 30 fps; ffprobe reports 150 frames / 5.0 s. clip001851 keyboard modeled visibility is >0.2 on 150/150 frames (min 0.740, median 0.883); component-wise GT translation range `[152.590, 194.500, 109.220]` mm, 3D range diagonal `270.265` mm. clip001849 control has component-wise range `[41.829, 73.973, 91.136]` mm, diagonal `124.609` mm. No local `/data2/ego_annotation_outputs/v19_runs/*clip001851*` prediction root exists; `/tmp` has review mirrors only. Decision: primary next physical mechanism is clip001851 R2 correspondence-first keyboard pose, with clip001849 as rigidity/control. Strict runtime boundary recorded: prediction may consume raw frames, prediction-side masks/depth/camera annotations, and optional prediction-side HaWoR; HOT3D GT object pose/hands/masks/proximity labels are evaluation-only after prediction freeze.

## 2026-07-06T12:21:40+08:00 — Clean-room review applied to clip001851 R2 preflight
Clean-room critic run `33495773` rejected the checkpoint only if `.memory/tasks/2026-07-06-research-track-execution/subagents/36_next_physical_slice_inventory.md` is staged unchanged, because that report still recommends a GT-aided pre-freeze correspondence probe using GT sidecar depth/pose as a track-lift input. Decision: do not stage report 36 in this checkpoint. The accepted mechanism boundary is the current `scripts/prepare_hot3d_r2_correspondence_preflight.py` and generated `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/preflight/a800_r2_run_contract.md`: runtime query-frame selection must be from prediction-mask evidence; GT frame 99 is diagnostic only; HOT3D GT object pose/hands/masks/proximity labels are post-freeze evaluation-only. Non-blocking reviewer checks: R0 wrapper writes only under `pose_state_schema_contract/`, A800 command flags match existing CoTracker/sparse/diagnose/fit CLIs, and motion/visibility numbers recompute correctly.

## 2026-07-06T12:28:30+08:00 — A800 path mapping fixed in R2 preflight
Post-commit inspection found the generated A800 run contract still used local `/data2/ego_annotation_outputs/...` paths. Intervention: updated `scripts/prepare_hot3d_r2_correspondence_preflight.py` to distinguish local verification root `/data2/ego_annotation_outputs` from A800 runtime root `/mnt/truenas-user-home/yiwen/ego_annotation_outputs`, add `a800_path_mapping` to `preflight_summary.json`, and generate the A800 contract with remote raw-manifest, GT-evaluation, and run-root paths. Validation: regenerated `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/preflight/a800_r2_run_contract.md`; assertion passed that `RUN_ROOT` starts with `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1`, no `/data2/ego_annotation_outputs/v19_benchmarks` path remains in the A800 contract, and `QUERY_FRAME_INDEX` still must be set from prediction-mask evidence.

## 2026-07-06T12:39:20+08:00 — A800 clip001851 R2 CoTracker stage launched
A800 host `yiwen@192.168.11.220` already had a prior prediction root `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260627_hot3d_clip001851_pinhole_a800_native_v1_supportgate` with 150-frame prediction-side keyboard SAM2 masks, UniDepth depth archive, and visible-geometry annotations. Built GT-clean R2 input artifacts under `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/`: `prediction_masks/keyboard_sam2_manifest.json`, `prediction_masks/query_frame_selection.json`, prediction-side symlinks `prediction_annotations/annotations_for_cotracker.json` and `prediction_depth/unidepth_metric_depth.npz`, and visual review sheet `prediction_masks/review/query_frame_prediction_mask_top12.jpg`. Query frame selected from prediction SAM2 mask area, not GT: frame 100, area 74,665 px; the review sheet shows a clean keyboard mask. Launched CoTracker only (no SAM2/UniDepth/TRELLIS rerun) via `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/run_cotracker_r2_keyboard.sh` using A800 model env `/mnt/user-home/yiwen/ego_annotation_remote/model_envs/unidepth_sam2/bin/python`, CUDA_VISIBLE_DEVICES=0, query-frame index 100, frames 0-149, max 384 points. Durable status/log: `logs/r2_cotracker_keyboard.status` initially `RUNNING 2327313 2026-07-06T12:39:20+08:00`, log begins with the START line. Prediction/evaluation boundary maintained: GT sidecar was not used to construct masks, depth, annotations, query-frame selection, or track inputs.

## 2026-07-06T12:41:12+08:00 — A800 CoTracker input-materialization failure fixed and relaunched
First CoTracker launch failed before tracking (`FAIL 1 2026-07-06T12:39:38+08:00`) because `prediction_annotations/annotations_for_cotracker.json` was a malformed relative symlink (`mnt/truenas-user-home/...` missing leading slash). This was an input-materialization failure, not evidence against the correspondence mechanism. Intervention on A800: replaced both `prediction_annotations/annotations_for_cotracker.json` and `prediction_depth/unidepth_metric_depth.npz` with real copied prediction-side files from the prior runtime root, making the R2 run root self-contained (111 MB annotations, 174 MB depth). Relaunched `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/run_cotracker_r2_keyboard.sh`; status `RUNNING 2351376 2026-07-06T12:41:12+08:00`; log shows cached CoTracker code is used. GT remains unused for runtime inputs.

## 2026-07-06T12:43:19+08:00 — CoTracker annotation schema mismatch fixed and relaunched
Second CoTracker launch failed before writing track outputs because `run_cotracker_object_tracks_v5.py` expects `camera.vggt_source_intrinsics_fx_fy_cx_cy`, while the prediction-side visible-geometry annotations carried `camera.intrinsics_fx_fy_cx_cy`. This was a schema-adapter defect. Intervention on A800: added `vggt_source_intrinsics_fx_fy_cx_cy` to all 150 copied prediction annotation frames, sourced from `intrinsics_fx_fy_cx_cy`, and recorded `vggt_source_intrinsics_added_for_cotracker_from`. Relaunched at `2026-07-06T12:43:19+08:00`; status `RUNNING 2374750`, child process `run_cotracker_object_tracks_v5.py` active with CUDA and cached CoTracker repo. No GT data was used in this adapter.

## 2026-07-06T12:43:45+08:00 — clip001851 R2 CoTracker measurement completed
A800 CoTracker stage completed successfully: `OK 2026-07-06T12:43:45+08:00`. Outputs under `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/r2_cotracker_keyboard/`: `cotracker_object_tracks_v5.npz` (576,842 bytes), `qc_cotracker_object_tracks_v5.json` (35,269 bytes), `cotracker_tracks_overlay.mp4` (8,191,909 bytes), and representative stills. QC summary from log: query frame 100, 128 query points, 8 all-frame accepted tracks, median valid frames per track 125, p05 76.5, p95 150; world-step median 44.9 mm, p95 292.3 mm; flow median 3.52 px, p95 25.21 px. Visual consumption sheet copied locally to `/tmp/clip001851_r2_cotracker_stills_sheet.jpg`: tracks visibly attach to keyboard at the query frame and cover the keyboard surface; long swept trajectories in high-motion frames make local rigidity residuals the next required discriminator before any pose/covariance claim.

## 2026-07-06T12:57:00+08:00 — track-only rigidity diagnostic on CoTracker correspondences
Ran CPU-light robust pairwise SE3 fitting on A800 using all accepted CoTracker object tracks, without sparse surface-edge filtering: `scripts/fit_cotracker_pairwise_rigid_factors_v6.py --cotracker-npz .../r2_cotracker_keyboard/cotracker_object_tracks_v5.npz --output-json .../r2_track_only_rigidity/fit_cotracker_pairwise_rigid_factors_v6_all_tracks.json --min-pair-tracks 12 --min-inlier-tracks 12 --huber-delta-m 0.010 --max-inlier-residual-m 0.012 --accept-inlier-p95-m 0.010 --irls-iterations 8`. This diagnostic uses no GT and does not certify surface support; it tests whether learned tracks contain any locally rigid material-motion windows before mesh support gating. Result: 128 usable tracks, 149 adjacent frame pairs, 9 rigid-factor-ready adjacent pairs. Ready pairs are isolated: 30-31, 57-58, 98-99, 121-122, 123-124, 128-129, 130-131, 144-145, 147-148. Ready-pair inlier residuals: count 520, median 5.38 mm, p95 9.56 mm. All-pair inlier residuals: count 4369, median 6.63 mm, p95 11.35 mm. Interpretation: CoTracker contains usable local rigid subwindows, but the current all-track measurement does not provide a contiguous rigidity-certified bridge or object pose trajectory; R4 solver_estimate rows remain parked until sparse visible-surface support and held-out residuals pass.

## 2026-07-06T13:12:00+08:00 — visible-surfel compatibility archive and sparse-edge semantic repair
Integrated critic decision `519995d8`: visible-surface sample archive is admissible only as a quarantined compatibility wrapper around prediction-side surfel vertices; faces are non-evidential loader padding; distances must be nearest visible-sample distances, not mesh/surface distances. Replaced the executor's mesh-named adapter with `scripts/build_visible_surfel_compat_archive.py`, which writes a loader-compatible NPZ with verbatim visible surfel vertices and one non-evidential placeholder face per frame, plus explicit forbidden/allowed claims and sample-spacing diagnostics. Repaired `scripts/build_cotracker_sparse_correspondence_edges_v5.py` report wording and fields to use `visible_surfel_archive` and `nearest_visible_sample_distance_m`; removed “repaired object mesh” and `surface_distance_m` from its output. Regenerated `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/preflight/a800_r2_run_contract.md`; the contract now builds `r3_visible_surfel_compat/visible_surfel_compat_archive.npz` and stops after sparse-edge inspection before rigidity. Synthetic local validation passed: `load_mesh_archive` accepts the archive, sparse-edge report emits 2 edges, and report JSON contains neither `repaired object mesh` nor `surface_distance_m`.

## 2026-07-06T13:18:00+08:00 — raw-mask CoTracker sparse-edge failure localized
Built the real clip001851 visible-surfel compatibility archive on A800: 150 frames, 375,000 vertices, non-evidential placeholder faces only; required frame validation matched CoTracker frames 0-149. Sample spacing median across frames is 3.62 mm; 18/150 frames have median spacing above the 4 mm edge threshold, so the threshold is a sample-proximity gate, not a continuous surface-membership test. Applying sparse visible-sample gating to the original raw-SAM2-mask CoTracker output produced 0 usable tracks and 0 edges; nearest visible-sample distance median was 1.12 m. Investigation showed CoTracker `tracks_xy` were in 960x960 manifest image pixels but were sampled directly in the 1408x1408 depth/intrinsics grid, producing false world points (e.g. frame 100 query point at image xy 720,384 sampled depth 2.79 m and lifted to z=1.84 m while visible surfels for the keyboard are around z=0.38 m). This was a coordinate-scale bug in `run_cotracker_object_tracks_v5.py`, not evidence against correspondence.

## 2026-07-06T13:28:00+08:00 — corrected visible-surfel CoTracker R2/R3 chain
Implemented `scripts/build_visible_surfel_raster_masks.py` and patched `scripts/run_cotracker_object_tracks_v5.py` so `tracks_xy` remain manifest image/mask pixels while `tracks_depth_xy` are scaled to the metric depth/intrinsics source grid before depth sampling and world lifting. Generated visible-surfel raster masks from prediction annotations with source radius 4 px scaled to output radius 3 px for the 960x960 manifest frames; 150 masks, median area 39,751 px. Reran CoTracker under `.../r2_cotracker_keyboard_visible_surfel_scaledepth/` with query frame 100, grid step 8, 384 points. Corrected world-step median is 10.33 mm (p95 31.00 mm), versus 47.85 mm median before scaling. Sparse visible-sample gate now finds 52 usable tracks, 3,598 edges, nearest visible-sample distance median 2.14 mm overall and 1.71 mm for usable tracks, edge world-step median 9.09 mm, longest single-track edge run 48 frames. Local rigidity diagnostics on sparse-supported tracks: 52 usable tracks, 148/149 adjacent pairs have enough tracks; rigid residual median 1.43 mm, p95 5.40 mm; pairwise length error median 0.98 mm, p95 6.93 mm. Robust pairwise factors: 147/149 adjacent pairs ready, ready-pair inlier residual median 1.40 mm, p95 4.99 mm; ready runs are 0-119 (119 pairs) and 120-148 (28 pairs). Non-ready pairs: 119-120 narrowly exceeds the 10 mm p95 threshold (inlier p95 10.65 mm), 148-149 has too few supported tracks. No GT was used in runtime inputs or diagnostics.

## 2026-07-06T13:40:00+08:00 — clean-room review accepted R2/R3 checkpoint and grid guard added
Clean-room adversarial review `caf83516-2fc8-45ae-b276-56bfcb838c8a` inspected checkpoint `0ab9d2e` and A800 run `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/`. Verdict: no blockers; checkpoint stands as GT-free R2/R3 diagnostic evidence only. Reviewer independently verified raw-mask sparse gate 0 usable tracks / 0 edges / 1.12 m nearest-sample median; raw pre-fix NPZ lacks `tracks_depth_xy` and frame-100 world z≈1.84 m; corrected scaled-depth visible-surfel run has 52 usable tracks, 3,598 edges, 2.14 mm nearest-sample median, 147/149 rigid-ready pairs, and rigidity residual median/p95 1.43/5.40 mm. Invariant review found no laundering: faces are one non-evidential placeholder triangle per frame and ignored by sparse-edge code; no mesh/pose/contact/occlusion/nonpenetration/GT claims are made; `annotation_ready=false` remains diagnostic. Non-blocking hardening note: `scale_xy_to_depth` assumed depth grid equals annotation/source intrinsics grid. Applied patch to `scripts/run_cotracker_object_tracks_v5.py`: `validate_depth_grid()` now raises if `source_height/source_width` are present and do not match the metric depth grid before depth sampling/world lifting. Validation: `python3 -m py_compile scripts/run_cotracker_object_tracks_v5.py` and a direct guard test passed (`depth_grid_guard_ok`).

## 2026-07-06T16:20:00+08:00 — R4 gauge/fusion boundary after R4a-R4d reviews
R4a hard-gated diagnostic script `scripts/fit_visible_surface_patch_se3_epochs.py` now enforces scale-corrected CoTracker input (`tracks_depth_xy` present), plausible clip001851 metric z range, same-run sibling inputs, visible-surfel provenance, GT quarantine, gauge-gate status, and epoch-break/no-stitch semantics. It always emits `diagnostic_only=true`, `annotation_ready=false`, `is_full_object_pose=false`, `camera_drift_separated=false`, and `pose_evidence_admissible=false`; even accepted gauge input leaves it a diagnostic unless a downstream consumer supplies its own valid covariance/gauge handling. R4b broad background gauge was rejected: background tracks were non-rigid/depth-diverse (`unresolved_nonrigid_background`, 24/149 ready pairs, 0/512 static tracks). R4c near-keyboard local gauge produced an `accepted_local_static_core` over frames 0-110 with artifacts under `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/r4c_local_static_gauge/`, but critic review found this proves a coherent near-field rigid field, not an independently validated camera/extrinsic gauge. R4d gauge-corrected object-vs-near-field motion reported population-level relative motion (supported 83 frames; 39 frames gauge-uncovered) but is accepted only as relative diagnostic evidence. It is rejected as object-pose evidence because (1) local desk-gauge staticness is not independently proven, and (2) proposed fusion with existing depth-ICP would use completed TRELLIS mesh ICP, reintroducing the geometry-prior phantom class rejected in clip001850. Next root blockers: independent prediction-side ego-motion/cross-depth gauge validation, and observed-surface/visible-surfel absolute backbone replacing completed-mesh ICP. No contact/occlusion/nonpenetration promotion follows from R4d.

## 2026-07-06T18:40:00+08:00 — clip001851 observed visible-surface render delivered
Built a full-duration observation-only render for clip001851 from prediction-side visible surfel samples, not from completed mesh or pose rows. Script: `scripts/render_visible_surfel_surface_artifact.py`; remote script copy: `/mnt/user-home/yiwen/ego_annotation_remote/repo/scripts/render_visible_surfel_surface_artifact.py`. Inputs: R2 run root `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/`, base run `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260627_hot3d_clip001851_pinhole_a800_native_v1_supportgate`, visible-surfel archive `r3_visible_surfel_compat/visible_surfel_compat_archive.npz`, prediction annotations/camera intrinsics, SAM2 keyboard masks, RGB manifest. Outputs under `render_visible_surfel_surface/`: `v19_overlay.mp4` 150 frames/5.0s, `v19_world.mp4` 150 frames/5.0s (camera-relative 3D scatter, not global world pose), `v19_side_by_side.mp4` 150 frames/5.0s, `v19_still_sheet.png`, `v19_render_manifest.json`, `v19_render_report.json`. Render semantics: observed partial visible surface samples only; points/splats are drawn from per-frame 2500 surfel vertices projected through the prediction camera for that same frame; NPZ faces remain non-evidential loader padding and are not rendered; no cross-frame accumulation; no completed/TRELLIS mesh; no object pose/contact/occlusion/nonpenetration claim. Validation: all videos have 150 frames and raw duration 5.0s; median projected-surfels-on-SAM2-mask fraction 0.9934 (min 0.9884, max 0.9988); representative frames visually/programmatically inspected. This changes the artifact surface by rendering observed object substrate, but it does not reduce the unresolved R4 gauge-independence or absolute-backbone blockers.

## 2026-07-07T01:05:00+08:00 — clip001851 non-circular visual prior and conditioned residual
Built and repaired the first non-circular contact-evidence chain for `clip001851`. Visual prior packet: `/data2/ego_annotation_outputs/research_clip001851_visual_contact_prior_20260706/packet/clip001851_visual_contact_prior_packet.json`; validator report: `/data2/ego_annotation_outputs/research_clip001851_visual_contact_prior_20260706/packet/clip001851_visual_contact_prior_validator_report.json`; review sheet: `/data2/ego_annotation_outputs/research_clip001851_visual_contact_prior_20260706/review/clip001851_visual_contact_prior_sheet.png`. The prior source modality is `rgb_frames_plus_prediction_mask_overlay`: raw RGB with prediction-side SAM2 keyboard tint only; no GT skeleton, no distance/error HUD, no MANO-object distance, no surfel residual, no object pose, no depth, no solver correction. Semantic prior hash is `236b1fe560dc54a109c3842df91a0d8baae4f93340f307c427d4a0d4191352c3`, computed as sha256 over canonical JSON excluding `prior_hash` and `validator_report`; raw packet file checksum is `005021bd692ea31f007065609e5053bf5d407b21ec8753b2e753f2ad38c38950` and is recorded only as an artifact checksum. Visual prior summary over 37 selected frames x 2 hands: right asserted/unresolved/absent = 16/20/1; left asserted/unresolved/absent = 23/14/0.

Conditioned geometry residual/render: A800 run root `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/`; residual report `/r5_visible_surface_proximity_residual/visible_surface_proximity_residual_state_conditioned_report.json`; render manifest `/render_visible_surface_proximity_residual/v19_visible_surface_gap_render_manifest.json`; videos `v19_visible_surface_gap_overlay.mp4`, `v19_visible_surface_gap_world.mp4`, `v19_visible_surface_gap_side_by_side.mp4`, still sheet `v19_visible_surface_gap_still_sheet.png`. Manifest and conditioned report now use `conditioned_on_prior_hash=236b1fe560dc54a109c3842df91a0d8baae4f93340f307c427d4a0d4191352c3`, `conditioned_on_prior_id=clip001851_visual_contact_prior_v1.0.0`, `prior_not_modified_since_compute=true`, `residual_is_likelihood_only=true`, `metric_mano_preserved=true`, `no_solver_correction=true`; raw SHA `005021bd...` appears only under `prior_packet_sha256`. All three videos are 150 frames / 5.0s; side-by-side is 1924x960. Active display labels are `visible_surface_gap_close`, `visible_surface_gap_near`, `visible_surface_gap_high`, `hidden_state_unresolved`; no active filename/row label uses the superseded `close_visible_surface_support`/`near_visible_surface_support` wording. The old support-named render files remain on disk only as superseded negative evidence. This closes the prior/residual circularity for the selected frames, but it still does not create `confirmed_contact`, object pose, nonpenetration, occlusion ownership, or hand correction.

## 2026-07-07T02:35:00+08:00 — clip001851 R6/R6c rejected as pose/contact evidence
R6 produced a visible-prior × visible-surface-residual posterior and a before/after candidate render, but parent inspection found the translation mechanism was per-row median(target visible surfel − source MANO sample) times posterior drive; it is a bounded surface snap, not an independent pose correction. R6c then attempted to rescue the branch with a right-hand constant bias from an R6b train/heldout discriminator. Independent Opus audit (`/tmp/clip001851_r6c_opus_critic.md`) rejects R6c as pose/contact evidence: R6b's own overall verdict is `H_bias_not_supported__do_not_accept_pose_correction`; the right-hand split is interleaved local interpolation over frames 93–98, not independent generalization; the constant bias worsens f94/f97/f98 and f94/f98 are training frames; train deviation from the right bias (median 17.1 mm, max 34.1 mm) exceeds the 10.1 mm bias norm; the apparent right/left split is threshold noise; best post-correction right gap is still 11.7 mm, so even the best-case correction remains near/unresolved. R6/R6b/R6c are negative diagnostics only. Local untracked script ports from the rejected R6b/R6c branch were removed. Next discriminating action launched as post-freeze evaluation-only run `a9df39f6`: HOT3D GT scoring of whether the R6c vector matches true HaWoR→GT metric error. GT must not mutate prediction artifacts.

## 2026-07-07T02:50:00+08:00 — R8 post-freeze GT scoring refutes R6c bias
Post-freeze evaluation-only scoring completed for the R6c right-hand global-bias hypothesis. Report: `/tmp/clip001851_r8_gt_bias_scoring.md`; JSON: `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/evaluation/r8_gt_bias_scoring/clip001851_r8_gt_bias_scoring.json`; rows TSV: `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/evaluation/r8_gt_bias_scoring/clip001851_r8_gt_bias_scoring_rows.tsv`; JSON SHA256 `0c9bdacde30ab191ab61337858896cd97cd32fb8e4467c4c6797d5a775ef8d3d`. Inputs were HOT3D GT sidecar `/data2/ego_annotation_outputs/v19_benchmarks/hot3d_clips/v19_inputs/clip-001851/evaluation/hot3d_gt/hot3d_clip_gt_sidecar.json` and frozen HaWoR source `/data2/ego_annotation_outputs/v19_benchmarks/hot3d_clips/v19_inputs_pinhole/clip-001851/measurements/hawor_world_pinhole_f610/hawor_world_hands.npz`. No prediction state was mutated.

Result: GT refutes the R6c vector `[+6.314,+4.591,+6.402]` mm world as true HaWoR→GT metric bias. On R6c target frames `[60,93,94,95,96,97,98]`, right MPJPE worsens `44.51 → 48.89` mm and right median vertex error worsens `44.39 → 48.29` mm; `0/7` rows improve. Median true right vertex offset in camera frame is `[-21.08,+19.46,-31.74]` mm while median R6c bias in camera frame is `[+6.76,+6.62,+3.52]` mm; median angle is `106.73°` and signed projection fraction `-0.0655`. Neighbor controls also worsen (`11.50 → 15.15` mm, `0/11` improved); all non-target right controls worsen (`15.39 → 20.62` mm; `19/143` improved); left same-bias counterfactual worsens (`168.48 → 177.91` mm, `0/7` improved). This confirms R6c is a coincidental visible-surface gap reducer/snap artifact, not a metric hand bias. The R6/R6b/R6c branch is closed as negative diagnostic only and must not be used for pose/contact annotation.

## 2026-07-07T03:05:00+08:00 — post-R8 route split: hand-source audit negative for right metric correction; DROID gauge candidate produced
Parallel post-R8 subagents completed. Theory (`/tmp/clip001851_post_r8_theory.md`) and critic (`/tmp/clip001851_next_mechanism_critic.md`) agree that confirmed contact is not GT-free reachable in the current state; the substantive reachable pose path is object/camera gauge, with hand-source work useful only if it deploys a GT-free source rather than re-auditing.

Hand-source audit (`/tmp/clip001851_hand_source_audit.md`) found a prediction-side aggregate improvement but no stable right-hand metric source. TrackK lateral-refit curve is best two-hand aggregate (`35.40` mm median MPJPE / `34.54` mm vertex-median) versus R8 HaWoR (`42.59` / `42.65`), mostly from left/visible-hand geometry. For full right hand, R8 HaWoR remains best (`18.61` mm MPJPE) versus TrackK curve (`24.91`), TrackI hybrid (`34.64`), and mask/depth refits worse. TrackK improves the narrow R6c target-right window (`35.40` vs R8 `44.51`) but worsens the later right tail (`50.72` vs R8 `42.23`), so it is window-local and not an independent global right-hand metric correction. No full raw WiLoR metric MANO NPZ for clip001851 was found; available WiLoR-derived sources are TrackI hybrid and TrackK variants.

Gauge route audit (`/tmp/clip001851_gauge_route_audit.md`) found no pre-existing DROID/DPVO/COLMAP camera trajectory for clip001851, then ran DROID full-frame on the A800 using prediction-side inputs only. Output root: `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/r4_camera_gauge_droid_full_frame/`; files include `droid_dense_trajectory.npz`, `droid_dense_trajectory.json`, `droid_qc.json`, `droid_keyframe_reconstruction.pth`, `droid_keyframes.json`, `droid_prediction_camera_validation.json`, `run.log`, `run.status`, and `run_command.txt`. DROID QC: 150/150 frames, 9 keyframes, runtime 39.6 s, raw path length 0.7929, median raw step 0.00322, p95 0.02107. No-GT validation: finite SE(3), rotation determinant median ~1, orthogonality error median `5.83e-08`, similarity DROID→existing prediction camera residual median `0.003553` m / p95 `0.007071` m / max `0.008572` m, adjacent step-length correlation `0.978085`. This reduces the R4 blocker from “no independent camera/gauge trajectory exists” to “DROID camera gauge candidate exists and must be accepted/rejected by observed-surface pose residuals.”

Current live attempt `c03f02d1` is tasked to consume DROID + visible surfel correspondences into an R4e observed-surface object-pose backbone with pose rows/covariance or a hard blocker. No GT, no TRELLIS/completed mesh, no contact-derived residual correction.

## 2026-07-07T04:05:00+08:00 — GT-free center-principal-point convention correction is negligible
Adapter/convention audit (`/tmp/clip001851_adapter_convention_audit.md`) found one lawful GT-free HaWoR/HOT3D convention mismatch: HaWoR assumes image center principal point while the HOT3D pinhole adapter preserves calibrated principal point. For clip001851 this gives a deterministic minimal camera-frame rotation of `0.363594°`, not the prior GT-derived ~3.8° Track-M rotation. Candidate artifacts were frozen under `/tmp/clip001851_hawor_center_convention_candidate/` with candidate NPZ SHA256 `24be10dbede32efe0709d8a8166f616c20456008033a3eaa9b3573c78dbc7178`. The audit found no lawful prediction-side source for a larger ~3.8° adapter/extrinsics correction; missing fact is a documented HaWoR learned-camera basis transform or independent prediction-side observation of the same variable.

Post-freeze evaluation-only scoring (`/tmp/clip001851_center_convention_gt_score.md`) compared frozen source HaWoR against the 0.363594° candidate. Outputs: `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/evaluation/center_convention_gt_score/clip001851_center_convention_gt_score.json` (SHA256 `14f7091be6cdeac2e41e9c073e8145aa1131a3ec804090c6c550eb7fb601ac22`) and TSVs/scripts in the same directory. Result: negligible. Full two-hand median MPJPE improves `42.594 → 41.628` mm (+0.622 mm) and vertex-median `42.648 → 41.834` mm (+0.616 mm), below practical significance. Full-left improves about 1 mm, but full-right slightly worsens (`18.607 → 18.791` mm MPJPE; vertex-median `15.900 → 16.468` mm). R6 target-right improves only 0.464 mm MPJPE. Candidate wrist/root motion is nearly orthogonal to true offset (full-both 88.47°, full-right 91.94°; projection fraction near zero). This closes the adapter/convention route as a major hand-metric correction: the real deterministic mismatch is too small to solve contact/pose, and the missing ~3–4° mechanism remains unobserved prediction-side.

## 2026-07-07T04:25:00+08:00 — R4e DROID observed-surface object-pose candidate produced; acceptance pending
R4e executor produced a full-duration DROID-gauge observed-surface keyboard pose candidate at `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/r4e_droid_observed_surface_pose_backbone/`. Main artifacts: `object_pose_droid_observed_surface_backbone.ndjson/json`, `reference_observed_surfel_body.npz`, `manifest.json`, `validation_summary.json`, builder script, `v19_overlay.mp4`, `v19_world.mp4`, `v19_side_by_side.mp4`, and review stills. Executor validation: 150 pose rows, 149 accepted numeric pose rows, frame 78 null/unsupported, finite covariance on accepted rows, all videos 150 frames / 5.0 s, no GT/completed mesh/MANO-contact residual/face geometry consumed. Heldout correspondence median: median 7.56 mm, max 23.49 mm; heldout visible-surfel median: median 3.42 mm, max 6.07 mm; translation sigma median/max 13.20/26.04 mm; rotation sigma median/max 5.02/9.91°. Review still `side_by_side_000100.jpg` shows a rendered keyboard body overlaying the keyboard; `side_by_side_000078.jpg` shows null/unknown with no body.

Independent R4e critic (`/tmp/clip001851_r4e_droid_pose_critic.md`) does not yet accept this as final object pose. Mechanism gaps: the DROID gauge is unmasked full-frame and may absorb keyboard/hand motion; DROID was validated mainly against existing prediction camera, a weak independent signal because HaWoR uses DROID-family SLAM; DROID is monocular scale-free and similarity-scaled to prediction camera; `run_droid_full_frame.py` uses centered principal point `(704,704)` rather than calibrated `(722.29,720.90)`, so principal-point bias is unbounded. The artifact is therefore a substantive object-pose candidate/render, not accepted final pose. Follow-up launched: `fbff4ac2` post-freeze GT scoring of R4e pose trajectory, and `3cd0a0c3` prediction-side masked/static gauge validation. Acceptance requires either gauge independence and GT-within-covariance support, or demotion to diagnostic.

## 2026-07-07T04:40:00+08:00 — R4e post-freeze GT score supports trajectory shape but refutes current covariance calibration
Post-freeze evaluation-only scoring completed for the frozen R4e DROID observed-surface keyboard pose candidate. Report: `/tmp/clip001851_r4e_gt_pose_score.md`; outputs under `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/evaluation/r4e_droid_pose_gt_score/`; JSON SHA256 `4ca66f047767ba29be5c1fd2cd333450f69b7b0071fb438f333c1a7a5c4ff5af`, rows TSV SHA256 `507d48a8420764fafc4f34353ecc3114066416548bd549fe4427992838006ae2`. GT keyboard mapping is object_bop_id 28. R4e has 149/150 accepted frames and explicit null at frame 78.

Raw R4e and GT are in different world/body-frame conventions (raw translation median 1041.66 mm; rotation median 131.23°), so primary evaluation uses a single post-freeze Sim(3) translation alignment plus constant SO(3) body-frame offset. After alignment, R4e translation error is 18.65 mm median / 46.76 mm p95 / 63.93 mm max over accepted frames; rigid-only alignment is 20.50 mm median / 76.12 mm p95; Sim(3) scale is 0.75049. Aligned rotation consistency is 5.59° median / 19.41° p95 after constant body-frame offset. R4e is modestly better than completed-mesh baselines after their own Sim(3) (pose-graph 24.03 mm median / 54.27 mm p95; visible-ICP 23.78 / 54.42) while avoiding completed-mesh ICP. It also beats the stationary oracle mean baseline tail (52.67 mm median / 169.29 mm p95), though a GT frame-100 hold has low median 5.18 mm and poor tail 222.15 mm. Covariance is under-calibrated: translation within 1σ/2σ/3σ is 27.5%/63.8%/88.6%; rotation 46.3%/76.5%/86.6%. Thus R4e supports a useful trajectory-shape candidate, but current covariance/gauge calibration is too optimistic and raw metric gauge remains unproven. Live masked/static gauge validation run `3cd0a0c3` remains required before promotion to accepted prediction-side object-pose backbone.

## 2026-07-07T04:55:00+08:00 — masked/static validation supports R4e gauge for uncertain pose rows
Prediction-side masked/static gauge validation completed for R4e DROID (`/tmp/clip001851_r4e_masked_gauge_validation.md`). Remote outputs under `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/r4e_masked_static_gauge_validation/` include `clip001851_keyboard_hand_masked_constant.mp4`, `masked_video_summary.json`, masked DROID root `r4_camera_gauge_droid_keyboard_hand_masked_constant/`, and `masked_static_gauge_validation_report.json`. The validation used prediction-side SAM2 keyboard + hand masks with 18 px dilation and constant gray fill; median/p95/max masked coverage was 12.30/13.37/19.05%.

Masked DROID completed 150/150 frames with 9 keyframes. Comparing full-frame vs keyboard+hand-masked DROID after independent alignment to the existing prediction camera: center delta median/p95/max `1.32/6.37/8.15` mm; step-vector delta median/p95/max `0.54/1.91/3.52` mm; incremental rotation delta median/p95 `0.034°/0.119°`. Keyboard visible-surface motion median/p95 was `9.91/29.94` mm, and correlation between keyboard motion and mask-induced camera-step change was only `0.167`, so there is no evidence that unmasked DROID is absorbing keyboard/hand motion at the scale needed to invalidate the trajectory. Near-keyboard static-field residuals are comparable or slightly better for DROID: existing prediction camera 13.86 mm, full DROID 13.38 mm, masked DROID 13.23 mm. Broad background remains noisy/depth-diverse (~54 mm) and is not decisive. Remaining limitations: constant-fill masking is not native feature masking; hand masks are prediction-side SAM2; DROID still used centered principal point `[704,704]` instead of calibrated `[722.29,720.90]` (24.9 px / 2.65° equivalent); monocular scale is fitted to prediction camera centers (full scale 0.5623, masked 0.5749), not independently metric. Verdict: DROID gauge is prediction-side admissible for uncertain observed-surface pose rows if these limitations are carried into covariance/metadata; it is not a calibrated absolute metric camera claim.

## 2026-07-07T05:05:00+08:00 — adapter/convention critic closes Track-M-style right-hand correction route
Clean-room adapter/convention critic completed at `/tmp/clip001851_adapter_convention_critic.md`. Verdict: a prediction-side hand-frame correction is admissible only if it is a fixed, GT-free symbolic frame-convention bug in the production path; Track M's per-clip rotations are GT-fitted drift/oracle artifacts, not deployable convention corrections. Mechanistic discriminator: a fixed convention bug preserves rotation angle across clips (directly or under camera-frame conjugation), but Track M angles vary `0.72° / 3.40° / 3.81°` across clips with differing axes, which refutes the strong fixed-convention interpretation. Even/odd stability is insufficient because it controls per-frame overfit, not prediction-side estimability. DROID/HaWoR agreement cannot validate a hand-frame convention because both share the SLAM-estimator convention class. Any correction must change prediction-side production code and improve a GT-free self-consistency observable such as MANO replay or 2D reprojection; it must not edit the evaluator/scoring transform. The right-hand adapter/convention route remains closed as a major correction for clip001851. The critic notes a separate possible left-hand convention candidate (large structured ~168 mm left-hand error) that would require a GT-free replay probe, but it does not reopen the current right-hand/contact route and cannot ground contact/object pose now.
