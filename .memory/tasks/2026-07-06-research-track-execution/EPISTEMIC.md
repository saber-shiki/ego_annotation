# Current epistemic model

The research task is a mechanism-resolution problem. The artifact that matters is renderable HOI annotation whose visible state is driven by measured geometry, pose, contact, occlusion, and hand-correction rows. A row or graph-health decision is useful only if it changes the contact/geometry state a renderer consumes.

The first concrete slice is HOT3D clip001850 keyboard, right hand, frames 28-48 in `/data2/ego_annotation_outputs/v19_runs/20260626_hot3d_clip001850_pinhole_a800_native_v5_focalfix_coordrigid_v1`.

Current observation: the previous triple-valued contact contradiction is resolved against the “wire observed-surface penetration” route. The interval MANO penetration channel is a signed query against the inflated completed mesh filtered to depth-supported faces, not a trustworthy keyboard-depth contact signal. The keyboard-masked, hand-quarantined depth surface refutes it on every mask-available frame: at f32 the hand is 144 mm in front of the keyboard depth surface with zero eligible penetrating vertices while the interval channel reports 82 mm mesh penetration; at f36 the hand is 81 mm in front with zero eligible penetrating vertices while the interval channel reports 107 mm mesh penetration and full-frame depth shows only a small non-keyboard leak. The completed TRELLIS mesh is non-watertight, 95.4% inferred hidden surface, free-space-uncarved, and about 8.4x too thick in its thin axis. The rigid pose graph remains inert (`nfev=1`, cost 0, no NP targets) while prior state marked annotation-ready.

The proximate mechanism for clip001850 is now `geometry_epoch_contaminated` plus hand metric depth bias, not M1/M5 contact evidence ready to wire into contact/NP. M1/M5 remains a graph-health/source-lineage diagnosis: solver, contact, and render consume different measured provenance hashes, so generic “support absent” is still wrong. But that diagnosis no longer authorizes using the interval penetration as contact. Contact state must be produced by keyboard-masked/quarantined depth evidence and per-vertex coherence, capped by the clip’s 25-30 mm systematic metric floor; distance-only evidence cannot confirm contact on this clip.

Per-frame kill-test route distribution for right hand frames 28-48: `pose_unresolved` on 28,29,37-44,47,48; `geometry_epoch_contaminated` on 30-35 and 46; `full_frame_depth_leak` on 36; `contact_candidate_keyboard_masked` only on 45, with one vertex within 6 mm of the masked keyboard depth surface. f32 is `geometry_epoch_contaminated`; f36 is `full_frame_depth_leak`. These states now drive `/tmp/clip001850_egohoi_contact_review/egohoi_review_f032.jpg` and `egohoi_review_f036.jpg`, where the viewer-visible annotation changes from the published `gap/penverts/UNCERTAIN` banner to the concrete unresolved mechanism.

The graph-health route `cross_solver_geometry_decoupled` must be derived from measured provenance hashes and filter states: interval `depth_npz` hash plus mask/quarantine/eligibility flags, contact/NP mesh and sign-mesh hashes plus watertightness, render consumed state hash, and non-max summary statistics. Authored source-family strings and max-of-max penetration are invalid. Missing provenance routes to `evidence_incomplete` before any cross-solver decision.

Next mechanism work is not more reconciliation. It is either: (1) repair the object geometry epoch by free-space carving / observed-face watertight sign mesh so the keyboard body becomes contact-eligible; or (2) repair the hand metric depth bias so the hand can be compared to the keyboard depth surface. Until one of those changes, clip001850 contact remains explicit unresolved state, not no-contact and not confirmed contact.
