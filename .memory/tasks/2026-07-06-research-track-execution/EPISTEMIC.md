# Current epistemic model

The research task is a mechanism-resolution problem. The artifact that matters is renderable HOI annotation whose visible state is driven by measured geometry, pose, contact, occlusion, and hand-correction rows. A row or graph-health decision is useful only if it changes the contact/geometry state a renderer consumes.

The first concrete slice was HOT3D clip001850 keyboard, right hand, frames 28-48 in `/data2/ego_annotation_outputs/v19_runs/20260626_hot3d_clip001850_pinhole_a800_native_v5_focalfix_coordrigid_v1`. That slice is now R0/R1-clean at the substrate level: contact remains unresolved with zero admissible confirmed frames, object pose remains observation-only on f30-36,f46, and every current render surface consumes the unknown-preserving pose table rather than P15/static-fill pose values.

Current contact conclusion for this slice: GT-free right-hand keyboard contact is **unresolved with zero admissible contact frames**. The prior route “wire interval observed-surface penetration into contact/NP” is rejected. The interval MANO penetration channel is a signed query against the inflated completed mesh filtered to depth-supported faces. Keyboard-masked, hand-quarantined depth refutes it on mask-available frames: f32 has zero eligible penetrating vertices and the hand is 144 mm in front of keyboard depth while the interval channel reports 82 mm mesh penetration; f36 has zero eligible penetrating vertices and is 81 mm in front while the interval channel reports 107 mm mesh penetration and a small full-frame non-keyboard leak.

Object geometry mechanism: the completed TRELLIS keyboard body is the source of the phantom. It is non-watertight, 95.4% inferred hidden surface, free-space-uncarved, and about 8.4x too thick in its thin axis. Observed-only body repair extracts 3221 observed faces and removes the TRELLIS completion from the contact body. Free-space carving removes 49,222 of 87,671 carveable faces, but the body remains open because the back/sides/bottom are never observed. This repair invalidates false penetration and improves the rendered body/provenance, but it does not create contact: observed-only unsigned distances remain 35.6-115.2 mm and keyboard depth gaps remain 74.7-179.3 mm.

Hand-depth mechanism: the contact-forcing hand-depth counterfactual is refuted. To move fingertips to the keyboard-masked surface requires frame-varying along-ray shifts of 83-202 mm (median 134 mm), far beyond the 25-30 mm systematic metric floor, and produces catastrophic 2D reprojection displacement (62-169 px median, 110-285 px max). Only f36 reaches a contact_candidate after such a shift, and even that breaks projection. Therefore an ~8 cm hand-depth correction is not a physically admissible route to contact.

Canonical `ego.hoi.contact_frame_detail/0.1.0` carries the honest per-frame states for the kill-test window: `pose_unresolved` on 28,29,37-44,47,48; `geometry_epoch_contaminated` on 30-35 and 46; `full_frame_depth_leak` on 36; `unresolved_incoherent_evidence` on 45. f45 was demoted from the kill-test script’s permissive `contact_candidate_keyboard_masked` because it is one thumb vertex at +5.86 mm on a near-depth outlier, with no compact temporally persistent patch.

Render-consumer mechanism: the published clip001850 videos were produced by a Stage-1 renderer that baked in the raw TRELLIS body, zero-by-construction `penverts=0`, and a bare `UNCERTAIN` state, then Stage-2 `publish_v19_render_artifact.py` stamped one static banner on every frame. Stage-2 cannot express per-frame contact states. A three-frame still consumer is QC evidence only. The artifact-changing consumer is the full-duration replacement render at `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/v19_contact_state_full_duration/`: 150 frames at 30 fps, canonical-named `v19_overlay.mp4`, `v19_world.mp4`, and `v19_side_by_side.mp4`, driven by the durable contact table and the durable repaired observed body. It defaults the 129 frames outside the contact table to `unresolved_evidence_incomplete`, renders f32/f36/f45 as `geometry_epoch_contaminated` / `full_frame_depth_leak` / `unresolved_incoherent_evidence`, draws the 3221-face observed body hash `6d4bcebfb76149a4...` as amber/open/hatched rather than the solid TRELLIS mesh, and removes the false `gap 39.2mm`, `penverts=0`, and bare `UNCERTAIN` claims from the replacement pixels/manifest. A generic parameterized consumer now reproduces the same artifact-level gates from durable inputs and enforces floor-independent provenance for any future `confirmed_contact` row.

Reproducibility state for this slice: the durable rows under `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/ego_hoi_sidecar/extensions/org.ego.hoi/0.1.0/clip001850_research/` record the contact table, geometry/body provenance, graph-health observations, motion-coupling verdict, current object-pose observation rows, visibility ledger, render-consumption hashes, pose-provenance purity audit, package render-surface purity audit, and reusable pose-state schema contract. These rows are evidence-preservation and reproduction paths. They are not a physical mechanism and do not add contact or localization evidence beyond the observations stated above. The primary package render points to `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/unknown_preserving_pose_render/`, which draws the keyboard body only on f30-36,f46. The earlier P15-posed full-duration/generic contact renders, static-gauge render, static-gauge table, and static summary are all under `/data2/ego_annotation_outputs/research_clip001850_contact_state_20260706/superseded_prior_laundering/` or manifest superseded lists as negative evidence because they wrote or consumed numeric pose values on unknown frames. No root-manifest current/advised render surface points to them.

Motion-coupling mechanism: the only admissible GT-free contact-promotion channel was tested and is negative on clip001850. Direct keyboard pose edges have a motion floor of 76.9 mm translation / 18.2° rotation. The fingertip approach peak at f36 has keyboard response only 8.6 mm, below the 20.2 mm window direct-edge median; Spearman object-motion vs approach speed is −0.25 (p=0.59); the largest object edge f46 is a withdrawal-time mask-reacquire/refit jump, not a hand impulse; closest approach f39 is interpolated and has no independent object measurement. Therefore no frame in f28-48 can truthfully populate `motion_coupling_onset`, and `confirmed_contact` remains unreachable GT-free. The only remaining adjudicator for this slice is HOT3D GT/R8, whose evaluator code exists but whose GT bundle is not on local disk.

Object-pose mechanism for this slice: the keyboard has measured pose observations only on f30-36 and f46; the apparent late motion is mask drift plus graph pass-through. The P15 keyboard pose graph is inert by construction: `solve_v19_rigid_object_pose_graph.py` optimizes correction deltas around raw ICP fits, but all non-nonpenetration residuals are homogeneous in the correction field, P15 ran with `constraint_report=null`, and the available P16 nonpenetration state has zero accepted correction targets. Thus `x0=0` is the optimum (`nfev=1`, `cost=0`) and the direct ICP rows pass through unchanged. f45 is unresolved/hand-occluded. f60/f75/f76/f77 are rejected mask-drift observations; f75 is the contaminated visible-geometry anchor itself, 199.5 mm from rest. KT-L2 refutes support recovery from existing masks: all 72 f78-149 visible-surface-ineligible frames are mask-drift delaminated, hand/depth-cleaned centroids remain 138-278 mm from rest (median 166 mm), and 0/72 recover consensus support. Therefore unobserved frames remain object-pose unknown. The rest-cluster reference `T_rest=[0.581,-0.064,0.345] m` with spread `[9.2,15.8,11.5] mm` describes f30-36,f46 and may be used to reject outlier measurements; it is not a hypothesis/prior for missing frames, not a trajectory fill, not contact evidence, and not render-localized body placement. Current pose rows carry numeric R/t only on the 8 measured frames; 142 frames carry null pose with reason. The current render draws the keyboard body only on f30-36,f46 and shows no localized body on f45, f60/f75/f76/f77, or f78-149. The pose-provenance purity audit scores current/original/static-fill purity as 1.0/0.0533/0.0533, with 137 imputed original pose rows and 142 forbidden original numeric rows detected; its render-surface extension first detected the two root-advertised P15-bodied renders as violations, then passes after those renders were moved to superseded negative evidence. Graph-health now records current pose-observation/render-state hashes, package render-surface purity (`current_surface_violation_count=0`), and the reusable pose-state schema contract (`pose_state_contract_satisfied`). Contact states remain unchanged (`f32=geometry_epoch_contaminated`, `f36=full_frame_depth_leak`, `f45=unresolved_incoherent_evidence`). A smoothed trajectory, mask quarantine, median over all 13 direct fits, graph-health-only row, hard static fill, current/advised P15-bodied render, and any use of rest-cluster reference as graph prior are ruled out. Remaining boundary: this mechanism does not localize the keyboard outside the 8 observed frames and cannot promote contact; HOT3D GT/R8, new perception, or a later factor graph with explicit latent variables and calibrated model priors is required to infer unobserved intervals.

The next primary physical slice is HOT3D clip001851 keyboard R2 correspondence-first object pose, with clip001849 as the rigidity/control probe. The mechanism difference from clip001850 is decisive: clip001851 has local raw video, full HOT3D GT sidecar for post-freeze scoring, HaWoR hands, 150/150 modeled-visible keyboard frames, component-wise GT object translation range `[152.6, 194.5, 109.2]` mm (270.3 mm 3D range diagonal), and a documented 38.85 mm depth-ICP baseline residual whose stationary repair worsens to 55.75 mm. That creates a real trajectory variable that temporal correspondence can observe. clip001849 is useful as a lower-motion control (component-wise range `[41.8, 74.0, 91.1]` mm; 124.6 mm diagonal) and non-contact/rigidity statistic probe, not the primary artifact-changing slice. The prediction/evaluation boundary is now explicit: the R2 runtime may consume raw pinhole frames, prediction-side SAM2 masks, prediction-side metric depth, and prediction-side camera annotations; HOT3D GT object pose, hands, masks, and proximity labels are evaluation-only after prediction freeze and must not enter prediction rows or masks. The current preflight artifact is `/data2/ego_annotation_outputs/research_clip001851_r2_correspondence_20260706/preflight/`, generated by `scripts/prepare_hot3d_r2_correspondence_preflight.py`, and its A800 contract requires correspondence tracks, rigidity windows, covariance/observability-bearing pose candidates, GT residual scoring after freeze, and a render driven by correspondence pose rows before any contact recomputation.

## Current claim

- `clip001851` now has a GT-clean R2 CoTracker measurement from prediction masks/depth/annotations: 128 query points from prediction-mask-selected frame 100, 8 tracks accepted over all 150 frames, median track support 125 frames. The overlay shows visible keyboard-surface attachment at query time, but the large world-step tail means correspondence cannot yet support pose rows until sparse surface support and pairwise rigidity residuals separate coherent material tracks from drift/depth jumps. OPS: A800 CoTracker completion entry 2026-07-06T12:43:45+08:00. Track-only robust SE3 fitting on the original raw-mask CoTracker output found nine isolated adjacent ready pairs, but sparse visible-sample gating exposed the real blocker: the tracks were lifted in the wrong pixel frame (960px track coordinates sampled against 1408px depth/intrinsics), producing meter-scale false world points. After scaling `tracks_xy` to `tracks_depth_xy` and rerunning from prediction-side visible-surfel raster masks, clip001851 has strong GT-free R2/R3 evidence: 52 visible-sample-supported tracks, 3,598 sparse edges, nearest visible-sample median 2.14 mm, rigidity residual median 1.43 mm / p95 5.40 mm, and 147/149 adjacent pairwise rigid factors ready in two runs (0-119 and 120-148). This supports temporal correspondence and rigidity-window evidence; it still does not create final object pose rows or contact claims until a covariance-bearing solver/evaluator consumes the factors. Clean-room review `caf83516` verified the numbers and found no laundering/blockers; the only hardening applied afterward is an explicit annotation-source-grid vs depth-grid assertion before CoTracker world lifting. OPS: entries 2026-07-06T13:12/13:18/13:28 and 2026-07-06T13:40.
- R4 status for `clip001851`: R4a exists only as a hard-gated visible-surface patch SE(3) diagnostic; it is never pose evidence by itself. R4b broad background gauge failed because the background world lift is non-rigid/depth-diverse. R4c found a near-keyboard local static core over frames 0-110, and R4d showed keyboard patch motion relative to that near-field field at population level, but this is still a relative diagnostic: local gauge staticness is not independently proven as camera/extrinsic drift, frames 111-149 are gauge-uncovered, and the proposed depth-ICP fusion backbone uses completed TRELLIS geometry. Therefore no `solver_estimate` object-pose rows or contact/occlusion/nonpenetration changes are admissible yet. The next decisive blockers are independent gauge validation and an observed-surface/visible-surfel absolute backbone that does not consume completed mesh ICP. OPS: 2026-07-06T16:20.
- Observation artifact for `clip001851`: a full-duration visible-surfel render now exists at `/mnt/truenas-user-home/yiwen/ego_annotation_outputs/v19_runs/20260706_hot3d_clip001851_r2_correspondence_a800_v1/render_visible_surfel_surface/`. It draws prediction-side observed partial surface samples per frame as points/splats in RGB overlay and camera-relative 3D. It is a geometry-substrate/observation artifact only: no completed mesh, no absolute object pose, no contact, no occlusion ownership, no nonpenetration, no cross-frame accumulation. This breaks the diagnostic-only loop by changing rendered pixels, but it does not resolve R4 world-pose blockers: independent gauge validation and observed-surface absolute backbone remain open. OPS: 2026-07-06T18:40.

- `clip001851` now has the first non-circular contact-evidence chain. The contact prior is a geometry-blind visual-semantic packet over selected frames (source: RGB plus prediction SAM2 keyboard-mask overlay, no GT/distance/pose/depth), hash-frozen with semantic prior hash `236b1fe560dc54a109c3842df91a0d8baae4f93340f307c427d4a0d4191352c3`. The MANO↔visible-surfel measurement is only a conditioned geometry likelihood/residual, not a prior and not a contact detector. Its conditioned report/render references the semantic prior hash and records raw packet SHA only as checksum. This resolves the chicken-and-egg failure mode for the current evidence chain: visual prior → geometry residual, never distance → prior. Current artifact labels remain `visible_surface_gap_*`/`hidden_state_unresolved`; no `confirmed_contact`, pose, nonpenetration, occlusion ownership, or hand correction is justified yet. OPS: 2026-07-07T01:05.

- `clip001851` R6/R6b/R6c are rejected as pose/contact evidence. R6 was a per-frame visible-surface snap. R6c's right-hand constant-bias rescue fails mechanistically: the heldout split is temporally interleaved local interpolation, the right bias worsens multiple applied frames including training frames, train deviation exceeds the bias magnitude, and the best post-correction gap remains near/unresolved. The only justified claim is negative: the visible prior + residual branch can manufacture small plausible-looking corrections without proving contact or pose. The next live discriminator is post-freeze HOT3D GT scoring of the R6c vector against true HaWoR→GT error; GT use is evaluation-only. OPS: 2026-07-07T02:35.

- Post-freeze HOT3D GT scoring refutes the R6c right-hand bias hypothesis. The proposed `[+6.3,+4.6,+6.4]` mm world correction worsens every R6c target frame against GT, with right median vertex error `44.39 → 48.29` mm and MPJPE `44.51 → 48.89` mm; the camera-frame correction points ~107° away from the true HaWoR→GT offset. Neighbor/control frames also worsen. Therefore the contact-prior/residual correction branch is confirmed non-evidential: it reduced visible-surface gaps only as a snap/proxy, not as real hand-pose correction. Do not reuse R6/R6b/R6c for prediction-side pose/contact annotation. Next prediction-side progress must come from independent hand metric evidence or the unresolved object-pose/gauge backbone, not from MANO↔visible-surface distance. OPS: 2026-07-07T02:50.

- Post-R8 route selection: hand-source audit does not provide an independent global right-hand metric correction. TrackK lateral-refit curve improves aggregate/left geometry but R8 HaWoR is still best on the full right hand, and TrackK's target-window right improvement fails to transfer to the later right tail. The substantive pose route is now object/camera gauge. A prediction-side DROID full-frame camera trajectory was produced on A800 for clip001851: 150/150 frames, 9 keyframes, finite SE(3), and strong no-GT similarity to existing prediction camera (median residual 3.55 mm, step-length correlation 0.978). This does not prove object pose, but it removes the previous absolute blocker “no independent camera/gauge trajectory exists.” The live R4e question is whether DROID + observed visible-surface correspondences can produce covariance-bearing keyboard pose rows without TRELLIS/completed mesh or contact residuals. OPS: 2026-07-07T03:05.

- GT-free adapter/convention audit found only a small HaWoR-center vs HOT3D-calibrated-principal-point mismatch: 0.363594°, not the suspected ~3.8°. Post-freeze scoring shows this correction is negligible: full two-hand MPJPE improves only 0.62 mm, full-right slightly worsens, and the induced wrist motion is nearly orthogonal to the true offset. No lawful prediction-side source for the larger Track-M rotation was found. Thus the hand adapter route is closed as a major metric correction unless a new independent camera-basis observation appears; it cannot support contact annotation. OPS: 2026-07-07T04:05.

- R4e produced the first full-duration moving keyboard-body pose candidate for `clip001851`: 149/150 covariance-bearing DROID-gauge observed-surface pose rows and full overlay/world/side-by-side renders; frame 78 remains null. This is a real artifact change relative to observation-only surfels, but it is not accepted final object pose yet. The open mechanism is gauge validity: unmasked DROID may absorb object/hand motion, its validation against HaWoR/VGGT is weakly independent, monocular scale is similarity-fitted to prediction camera, and the DROID wrapper used centered principal point instead of the calibration contract. Live tests: post-freeze GT scoring of R4e and prediction-side masked/static gauge validation. OPS: 2026-07-07T04:25.
