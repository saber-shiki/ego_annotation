# EPISTEMIC — Pipeline V19

## Current model

The user-corrected Workbench is authoritative. V19 progress means a fresh-video pipeline whose rendered overlay/world/side-by-side artifacts are driven by physical mechanisms. Cached V16/V17/V18 roots are not valid default inputs; V18 code can be reused only as extracted mechanisms fed by V19 run-root measurements.

The active run root is `/data2/ego_annotation_outputs/v19_runs/20260624_task5_fresh_v19_pipeline_v0`. Workbench item 4 has produced a usable provisional interval artifact for task5 frames 690–725 after repairing the systematic focal/cache error; it is not accepted contact closure. Workbench item 5 now has a published calibrated f1083 uncertain render under canonical `v19_*` names. Workbench item 6 now has a fixed three-clip HOT3D-Clips 2D hand-box comparison slice (`clip-001849`–`clip-001851`); the supported external claim is only 2D hand-box localization, not 3D MANO/contact/object-pose performance.

The rigid tomato branch is coherent enough to hold fixed while repairing MANO. The fresh V19 run produced raw frames, UniDepth, calibrated HaWoR, DROID/camera evidence, SAM2 object masks, base annotations, visible geometry, TRELLIS completion/adaptation, visible-frame pose fitting, and a corrected rigid pose graph under the V19 run root. The tomato mesh/pose remains the best-supported object mechanism; object translation slack is rejected because it would move a visually plausible rigid tomato to hide hand errors.

The dominant accepted-closure failure is localized to MANO hand state and hand-owned contact/occlusion evidence. Calibration/focal/cache was a real systematic error and has been repaired enough to remove the gross f600 hand/object depth separation. The remaining failure is not solved by semantic contact priors, object pose, or small image-preserving depth shifts. It requires reliable hand-part/occlusion/contact evidence, especially for the left hand whose visible SAM2 support is forearm/occlusion rather than contacting hand surface. This is a blocker for claiming accepted physical contact, not a blocker for using the current render as an honest uncertain V19 artifact.

## Supported claims

- The frame-270 rigid tomato branch is the best supported object mechanism in the fresh V19 run: clean anchor evidence, TRELLIS completion, observed-surface adaptation, visible-frame pose fitting, and a corrected pose graph with sub-millimetre to millimetre-scale object corrections. Use it as fixed reference for MANO interval tests.
- The V19 calibration contract is now an explicit run-root source of truth. The current task5 hypothesis is constant `K=[1081.6570,1085.2280,969.7734,527.6045]`, focal 1083.441 px. Dataset-local ground-truth calibration remains absent; 600 was a V19 config/cache regression and 2304 is not ground truth.
- HaWoR caches are focal-dependent. The same-video-path f1083 rerun was rejected because QC focal changed while camera-space hands stayed in the f600 regime. The isolated focal-specific rerun under a new video/seq folder changed frame-720 hand depth from ~0.37 m to ~0.59/0.54 m and is the valid calibrated HaWoR input.
- Calibrated f1083 rebuild materially improved the rendered interval. Frame-720 world render moved MANO from gross separation to contact range. Interval metrics improved from the earlier ~9–13 cm gap regime to calibrated median contact gaps around 12.1 mm left and 24.3 mm right. This supports calibration/cache as a systematic error source that has been reduced.
- Calibration alone did not close item 4. The calibrated interval still uses near-limit visible joint shifts (~14 px), large hand translation/depth deltas (left ~7 cm, right ~4.5 cm median), zero selected visible-surface depth-order MANO vertices, and right active set non-closure. The artifact remains `MANO UNCERTAIN` and is not an accepted physical grasp.
- `scripts/build_v19_mano_mask_depth_refit_inputs.py` and `scripts/apply_v19_mano_mask_depth_refit.py` now implement a V19 mask-depth MANO refit instrument. It converts V19 world MANO/bridge state to a camera-frame refit contract, filters SAM2 hand masks by calibrated MANO projection minus object mask, and promotes only scale-preserving fits representable as MANO pose plus one translation into copied V19 annotations/bridge/source arrays.
- Hand SAM2 masks are evidence only when they cover actual visible hand surface. Visual review of `/tmp/v19_mask_depth_refit_filtered_hand_masks_review.jpg` shows the right filtered mask covers visible palm/fingers over 700/720/725, while the left filtered mask is mostly forearm/wrist. Left direct mask-depth refit is therefore rejected as a false hand-surface target.
- Unconstrained right mask-depth refit is artifact-changing but not accepted. It promoted 35 replayable right frames at fixed scale, improved right median contact gap from 24.3 mm to 17.6 mm and active residual from 8.4 mm to 2.2 mm, but required large image/pose changes (median joint reprojection change ~71 px; pose delta ~1 rad) and produced unstable/separated right-hand world geometry by frame 725.
- Conservative right mask-depth refit is rejected as a repair. It preserved image evidence much better (median joint reprojection change ~7 px, pose delta <0.45 rad) but worsened downstream contact gap/residual and still hit the visible-shift cap. This falsifies the hypothesis that a small image-preserving depth/root correction is enough.
- The current user-facing V19 render is now the calibrated f1083 uncertain branch, not the older contact-similarity branch. `scripts/publish_v19_render_artifact.py` published full-duration `v19_overlay.mp4`, `v19_world.mp4`, and `v19_side_by_side.mp4` with a color/metric legend under `/data2/ego_annotation_outputs/v19_runs/20260624_task5_fresh_v19_pipeline_v0/renders/v19_calibrated_f1083_uncertain_published_v1/`; canonical symlinks point there. The banner explicitly distinguishes usable uncertain MANO/object artifact from accepted contact closure.
- `scripts/build_v19_hot3d_clip_adapter.py` adapts public HOT3D-Clips WebDataset input into a V19 input video/manifest plus an evaluation-only GT sidecar. The current fixed HOT3D slice v1 is the first three `train_aria` tar files in repository path order: `clip-001849`, `clip-001850`, `clip-001851`. Each produced a 150-frame 1408x1408 input under `/data2/ego_annotation_outputs/v19_benchmarks/hot3d_clips/v19_inputs/`.
- `scripts/evaluate_v19_hot3d_hawor_boxes.py`, `scripts/render_v19_hot3d_hawor_box_review.py`, and `scripts/aggregate_v19_hot3d_box_evals.py` provide the first bounded external metric: 2D HaWoR hand-box localization against HOT3D amodal hand boxes. Aggregate on the fixed three-clip slice: 898 measurable hand rows, 871 matched, match rate 0.9699, median IoU 0.7027, mean IoU 0.7016, median center error 17.6 px on 1408 px frames. By side, left match rate 0.9844 / median IoU 0.7084; right match rate 0.9554 / median IoU 0.6987. Review sheets for sampled frames show predictions remain on the correct hands; clip-001850 demonstrates the main failure mode as missed right-hand rows rather than side swaps.
- `scripts/build_v19_hot3d_pinhole_adapter.py` now implements the HOT3D RGB fisheye-to-pinhole camera adapter using HOT3D/hand_tracking_toolkit equations. It consumes only `cameras.json` sensor calibration, writes a V19 pinhole input/manifest and calibration contract, and leaves hand/object/MANO/contact GT as evaluation-only. It produced 150-frame pinhole inputs for fixed-slice clips `clip-001849`–`clip-001851`; visual reviews show reduced fisheye curvature with no fold-over/axis swap/black-center failure. This supports a camera-adapter component claim and enabled pinhole HaWoR prediction.
- `scripts/evaluate_v19_hot3d_hawor_mano3d.py` and `scripts/aggregate_v19_hot3d_mano3d_evals.py` now provide the first bounded HOT3D 3D MANO hand-state metric. Mechanism: replay HOT3D GT MANO with side-specific MANO models, transform HOT3D GT and HaWoR prediction into camera coordinates, map HOT3D GT joints into HaWoR's exported 21-joint order, and report absolute wrist/joint errors separately from wrist-subtracted translation-aligned errors. No rotation/scale Procrustes alignment is applied, so the wrist-subtracted value is not pure articulation error. Fixed-slice pinhole aggregate: 900 measurable/matched valid MANO rows; 861 same-frame detector-supported rows and 39 infilled/not-same-frame rows. Overall medians: wrist 25.8 mm, joint MPJPE 32.9 mm, wrist-subtracted MPJPE 22.9 mm. Same-frame rows are much better than infilled rows (median joint MPJPE 31.6 mm vs 106.1 mm; wrist-subtracted 22.9 mm vs 72.8 mm). Left hand is weaker than right (median wrist-subtracted 34.6 mm vs 20.4 mm). This supports a 3D MANO hand localization/articulation claim on the fixed HOT3D pinhole slice, not contact/object/occlusion claims.
- Raw-fisheye vs pinhole HOT3D ablation supports the camera adapter as a causal mechanism. On the same fixed clips and 3D MANO metric, raw-fisheye HaWoR medians were wrist 78.9 mm, MPJPE 71.7 mm, wrist-subtracted MPJPE 29.7 mm, and vertex-centroid 70.0 mm; pinhole reduced these to wrist 25.8 mm, MPJPE 32.9 mm, wrist-subtracted 22.9 mm, and centroid 28.5 mm. The larger absolute-placement improvement than wrist-subtracted improvement means raw fisheye caused a systematic camera/input-model placement error, while residual errors are still hand-pose/articulation/infill/side-specific failures. This is support evidence for the HOT3D input path, not the primary MANO-correction ablation.
- The correct item-7 comparison is now HaWoR pinhole MANO baseline versus V19 MANO interval correction. `scripts/repair_v19_mano_support_temporal.py` implements a no-GT support/pose-gated temporal repair over prediction-side low-support intervals. The broad full-surface interpolation variants were rejected/limited because they overrepaired non-target spans or moved wrist/root harmfully. The best current variant, v5 wrist-relative repair, preserves framewise wrist/root and interpolates wrist-relative MANO surface/articulation over two left-hand low-support intervals. HOT3D fixed-slice result is weak positive but insufficient: overall MPJPE 32.86 -> 32.57 mm, left MPJPE 42.15 -> 40.56 mm, left vertex-centroid 37.43 -> 35.73 mm, right unchanged, but left wrist-subtracted aggregate does not improve. Target interval 001850 left 62-115 improves MPJPE 106.2 -> 94.5 mm and wrist-subtracted 81.9 -> 60.7 mm; 001851 left 86-129 improves wrist-subtracted 47.0 -> 43.6 mm but MPJPE stays ~70.5 mm. This supports temporal articulation prior as a component, not an accepted MANO correction.

## Rejected mechanisms and false prior claims

- Rejected: object pose jitter as the dominant 690–725 failure. The rigid tomato is plausible and object graph corrections are tiny; moving the object enough to close MANO gaps is physically false.
- Rejected: hard hidden/completed-volume nonpenetration from the tomato mesh. The completed mesh is non-watertight and hidden/free-space conflicts remain, so hidden volume is uncertainty evidence only.
- Rejected: raw projected-joint UniDepth as a direct hand-depth target. It can reduce proxies only by causing large image drift or is rejected when image evidence is preserved.
- Rejected: semantic contact/occlusion judgment as the sole missing factor. Agent-native contact priors are explicit and consumed, but rendered MANO/contact remains uncertain.
- Rejected: uniform hand similarity-scale repair as accepted MANO correction when it saturates scale. It is diagnostic only.
- Rejected: left mask-depth MANO refit from current SAM2 masks. The visible left mask is forearm/wrist, not the occluded/contacting hand surface, so using it would manufacture a hand state.
- Rejected: conservative right mask-depth refit as sufficient correction. It preserves image evidence but worsens physical contact metrics.

## Live uncertainties

- Right visible hand surface evidence is useful but incomplete. The unconstrained right refit shows that SAM2+UniDepth contains a signal that can reduce median contact gap, but broad masks and absent keypoint/part ownership let the hand drift anatomically and temporally.
- Left hand contact/occlusion remains unresolved. The visible evidence in 690–725 does not expose enough left hand surface; the needed mechanism must infer or carry uncertainty through occlusion/contact rather than fit forearm pixels.
- The current interval solver can optimize MANO root translation, root/wrist orientation, and articulation from a replayable source, but it lacks a trustworthy per-vertex hand-owned visible-surface/occlusion/contact observation family. Visible-surface depth-order rows select zero MANO vertices in the calibrated branch.
- HOT3D now scores 2D hand boxes and 3D MANO hand localization/articulation on the fixed three-clip pinhole slice. It still does not score contact, occlusion ownership, nonpenetration, object pose, or full V19 physical pipeline performance, because those require V19 object/contact state on HOT3D clips and a declared GT comparison adapter for those variables.
- H2O and DexYCB adapters remain unimplemented. They should not be opened unless HOT3D is blocked or after a stronger HOT3D physical-state adapter exposes a metric claim family that HOT3D cannot cover.
- Renderer/state naming still uses extracted `v18_*` components. This is acceptable only while they consume V19-produced inputs and the final render honestly shows uncertainty.

## Next discriminating action

The immediate active blocker is a valid clean HOT3D `clip-001850` runtime prediction, not scoring. The repaired no-infrastructure runtime bundle `hot3d_clip001850_bundle_20260625_v9_remote_input` proved the key remote-input mechanism through P04 on fresh run root `/data2/ego_annotation_outputs/v19_runs/20260625_hot3d_clip001850_pinhole_runtime_remoteinput_v1`: P03 UniDepth used the staged remote raw-frame manifest and copied back 150-frame depth/QC; P03b wrote a calibration contract with focal `559.1640985505215`; P04 staged the pinhole video remotely, forced HaWoR focal-cache refresh, produced local `measurements/hand_candidates/hawor_world/hawor_world_hands.npz` and QC status `ok` with valid hand frames left/right 150/150 and same-frame detector support left/right 134/138. The run then blocked at P07 before object masks/tracks: P05/P06 wrote a rigid `keyboard` object plan and point prompts, but P07 failed during prompt rsync because the remote nested `measurements/object_candidates/object_point_prompts_agent` parent did not exist; SAM2 itself did not run. The current spec repair is remote `mkdir -p` for P07 prompt/output parents plus neutral launch-contract wording. Relaunch must use a fresh run root/bundle and may reuse the logical repaired spec, not manually continue this blocked prediction. Only after the runtime freezes full prediction outputs and full-duration renders may HOT3D evaluation/scoring resume.

After a frozen runtime prediction exists, the next Workbench item-7/autoresearch mechanism should use stronger prediction-side hand-owned visible-surface/mask-depth evidence, not more temporal smoothing, because wrist-relative temporal repair was only weakly positive. On HOT3D this means deriving a visibility/surface proxy from images/masks without consuming GT, then comparing corrected MANO against the HaWoR pinhole baseline on the same fixed metrics. On task5 this points back to calibrated f1083 hand masks/depth: preserve the rigid tomato and focal contract, reject left forearm masks as contact evidence, and seek hand-part/occlusion-aware surface support before rerendering. Do not report contact, occlusion, nonpenetration, or object-pose metrics from HOT3D until those state variables are predicted by V19 and compared with a declared adapter.
