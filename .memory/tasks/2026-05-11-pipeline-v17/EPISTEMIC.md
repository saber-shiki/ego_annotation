# EPISTEMIC ledger — pipeline v17 (append-only)

2026-06-11 23:18 Initial state on rehydration (from PROMPT.md workbench + LOG.md tail).
- Live claim: pose-enabled full-residual relinearized hand graph accepts 1,072/2,927 measured hand rows; 1,614 residual candidates remain. Surface-tail diagnostic shows 743/772 persistent tail rows pass local surface geometry while 579 reject source depth in favor of nearby compatible seed pixels. This falsifies local MANO surface fitting as the dominant missing mechanism.
- Leading hypothesis: the missing mechanism is depth-observation/camera-depth ownership — which depth pixels a hand surface point should be compared against (occlusion, mask bleed, UniDepth edge error, calibration), not larger pose/ray-shift capacity.
- Competing mechanisms still alive: (a) per-pixel depth-observation assignment error (hand compared against background/far-field depth); (b) systematic camera-depth calibration/scale error per clip or per region; (c) residual MANO surface/pose error not expressible under the 0.35rad clamp; (d) object-contact occlusion (rejected as sole owner: only 151/1,433 depth-observation candidates are object/occluder rows).
- Object side: accepted BundleSDF pink-lid meshes cover short segments only and have zero shared-depth/contact-ready frames with current hand state. Full-interval contact-compatible object geometry absent. Coupled requirement, deferred behind hand depth ownership per dependency order.
- Readiness flags all false; nothing in current evidence supports closure.
- Next discriminating step: a solver iteration that jointly owns hand ray depth, MANO surface/pose, and per-sample depth-observation ownership, judged by full MANO reprojection + UniDepth resampling on trash_1050 and task5_tomato_960.

2026-06-11 23:35 Depth-owner diagnostic evidence (OPS 23:35). Over 1,926 pose-enabled full-residual variables: 714 studied depth-owner rows (persistent surface tail + local geometry pass + source-depth rejected). 602 have unassigned residual pixels; majorities: 536 far-from-object, 592 hand-in-front, 533 far-field+hand-in-front, only 33 near-object, 11 hand-behind. Unassigned pixel gap median -0.313m (hand in front of UniDepth, i.e., UniDepth sampled background); assigned-pixel gap median -0.183m; nearest compatible depth pixel median 10.8px (trash) / 10.4px (tomato), just outside the 8px assignment radius.
- Interpretation: pattern matches depth-edge bleed/misalignment at the hand silhouette boundary — UniDepth cannot represent the hand at boundary pixels, so the acceptance predicate (median/p95 over ALL projected hand pixels) penalizes correct hand states. Object-contact occlusion rejected as dominant owner (33/714). Camera-depth calibration disfavored: interior pixels pass local geometry; a calibration error would shift interiors too.
- This raises a measurement-validity hypothesis: metric_depth_compatible acceptance violates the depth-edge visibility invariant. Candidate decision class: "repair as measurement bug" — but only via a hand-independent ownership signal (UniDepth depth-discontinuity edge bands), to avoid circular acceptance.
- Discriminating prediction: re-evaluating the CURRENT pose-enabled solution with interior-owned (edge-band-excluded) pixels should flip a large fraction of the 714 studied rows to compatible WITHOUT changing hand state if edge ownership is the mechanism; if interiors still disagree, mechanism is calibration or MANO state and edge gating will not move acceptance.

2026-06-11 23:50 Predictions for depth-edge ownership counterfactual (before running). Measurement: recompute acceptance on the CURRENT pose-enabled full-residual solution, excluding UniDepth pixels inside depth-discontinuity bands (local max-min depth range > threshold within radius window; computed from UniDepth only, hand-independent).
- H1 depth-edge ownership (favored): >50% of the 714 studied rows flip to interior-compatible with unchanged hand state; most non-flips become interior-unobserved (UniDepth lacks the hand entirely, e.g., motion blur). Already-accepted 1,072 rows stay compatible (ownership exclusion must not destroy prior acceptance).
- H2 camera-depth calibration: interior gaps remain sign-biased; <20% flips; flip pattern uniform over depth, not concentrated at boundary-heavy rows.
- H3 residual MANO surface/pose: interiors still fail with mixed sign and large projection-to-seed offsets; flips <20%.
Decision rule: H1 confirmed -> classify legacy all-pixel acceptance as measurement bug; build the next full-residual solver iteration with edge-aware depth-observation ownership in both factors and acceptance, report legacy and ownership-aware predicates side by side. H1 falsified -> drop edge gating, target calibration/MANO mechanism instead.

2026-06-11 23:58 Edge-ownership counterfactual result vs predictions (OPS 23:58).
- Observed: 525/1,926 applied rows flip to interior-compatible; among 714 studied depth-owner rows, 331 flip (46%), 362 stay interior-incompatible, 21 unobserved. 47/241 legacy-compatible rows LOSE acceptance interior-only (edge pixels were balancing their medians). Studied rows have median edge-owner pixel fraction 0.41.
- H1 (edge ownership) partially confirmed, not as predicted: predicted >50% flips with remainder unobserved; got 46% flips and a large still-incompatible class. Edge ownership is a real measurement mechanism but not the only one.
- New anomaly: studied still-incompatible rows have interior median gap +23.6mm (p95 +42mm) — a small consistent positive bias (hand slightly behind UniDepth), just over the 30mm acceptance threshold, versus the -310mm edge-pixel gaps. The 1m-scale contradiction is gone on interiors. Remaining interior_still_incompatible split: 543 hand_behind, 409 tail, 95 hand_in_front.
- Interpretation: persistent residuals are now two cheap mechanisms: (a) depth factors/targets in the current graph were contaminated by edge pixels, so solved ray shifts are biased; (b) a residual +2-4cm interior offset that a per-row/temporal ray-shift variable can absorb once factors are interior-owned. H2 1m-scale calibration is dead; H3 large MANO error not required for the median class (tails may still need it).
- Decision: classify all-pixel depth acceptance/factor targets as a measurement bug (violates depth-edge visibility invariant). Next intervention: full-residual solver iteration with interior-owned (edge-band-excluded) depth factors and ownership partitions, re-solved and judged by full MANO reprojection + UniDepth resampling under BOTH legacy and interior predicates.
- Prediction for solver iteration: interior-owned re-solve keeps most of the 525 flipped rows compatible and converts a majority of the 362 studied still-incompatible rows (median +23.6mm within ray-shift reach); failure mode is tail rows (p95>80mm) needing MANO shape, and rows with <12 interior pixels. If interior re-solve does NOT close the +23mm class, the bias is in MANO surface/scale, not ray depth.

2026-06-12 00:20 Interior-owned solver, trash_1050 partial result (OPS 00:20). After re-solving per-row ray shifts against interior-owned UniDepth factors (pose deltas fixed from pose-enabled graph): interior-compatible variable rows 150(source, interior measure) -> 499/843; interior median gap ~0mm (p95 ±3-7cm); only 75/843 bound hits; legacy all-pixel acceptance dipped 762->734 as predicted when edge pixels stop driving the objective. Matches H1 solver prediction (EPISTEMIC 23:58): the +23mm interior bias was within ray-shift reach. Remaining incompatible classes: 203 tail, 71 in-front, 41 behind, 29 projection-untrusted. Tomato pending; no closure claim until both cases and joint contract regenerate.

2026-06-12 00:35 Interior-owned solver full result, both clips (OPS 00:35).
- Observed: 1,926 variables; interior-compatible variable rows 1,270 (66%); interior median gap +0.16mm, p05/p95 ±2cm; only 77 bound hits. Per-case interior acceptance (vars + nonvars): trash 1,056, tomato 940 -> 1,996 total vs 1,072 legacy-accepted before. Legacy all-pixel predicate on the same state: 1,022 (down from 1,072) — expected, since edge pixels no longer drive the objective and legacy medians include edge-band background gaps.
- Prediction check (EPISTEMIC 23:58 + 00:20): confirmed. The +23mm interior bias was absorbable by per-row ray-depth once factors became interior-owned; falsifies "MANO surface/scale owns the median class".
- Remaining interior-incompatible: 504 tail (p95>8cm; MANO shape/articulation or residual edge contamination), 74 in-front, 49 behind, 29 projection-untrusted. These are the true hand-side residual classes under a visibility-valid measurement.
- Scope limits: interior predicate validity rests on the edge-band parameters (7px/0.10m/6px dilation) being a fair visibility model; not yet validated against an independent hand silhouette source. Legacy/interior disagreement (1,022 vs 1,996) is itself evidence the acceptance predicate, not just the solver, was a bottleneck — the joint contract now records both.
- Pipeline implication: hand-side blocker has moved from "depth ownership unknown" to "interior-owned hand state mostly consistent; residual 656 rows + predicate validation remain". Object geometry/contact ownership becomes the highest-leverage blocker for V17 closure again (dependency order item 2/3).

2026-06-12 (object-side intervention design) User correction: reporting hand-side closure as session output mistook a dependency for the task. Task = full V17 deliverable videos. Binding blocker: full-interval object geometry/pose.
- Observation: v17 reconstruction jobs are derived only from observed-surface material-motion seed windows (2 short pink-lid segments). But full-interval per-object RGBD+mask datasets already exist under v17_object_track_datasets (trash: 4 objects, 232-437 frames each; tomato: tomato 655, peel 217, faucet 62). BundleSDF is built for exactly this input (full-sequence unknown-object 6-DoF tracking + neural SDF reconstruction); the seed-window restriction was an artifact of the material-correspondence evidence chain, not a backend requirement.
- Intervention: full-active-interval BundleSDF jobs per exported object, same rectification contract, run on A800 (GPUs 0/2/3 free).
- Predictions: (a) rigid objects (pink-lid can, off-white can, faucet) -> full-interval pose sequences + mesh passing the same projection/depth QC as the accepted short segments; (b) deformable objects (bags, tomato peel) -> tracking may hold but mesh/depth QC fails or drifts, which would be evidence that deformation needs explicit state, not a solver bug; (c) tomato (655 frames) is the runtime risk.
- Falsification: per-frame ob_in_cam coverage over the full exported interval + rasterized front-surface depth QC. If pink-lid full-interval job fails where short segments passed, interval length/gap robustness is the mechanism to study next.

2026-06-12 01:10 Full-interval job export, first pass evidence. 7 jobs; only faucet (62f) and tomato_peel (217f) pass the job-level rectification contract. Failures are frame-level outliers, not systematic: tomato 6/655 bad frames (worst inside-fraction 0.07), white bag 76/432, pink-lid 21/232. Mechanism: per-case UniDepth intrinsics vary widely across frames (fx spread up to ~300), so a single median target camera clips object points on extreme-intrinsics frames. Job-level min() acceptance was the wrong predicate for full intervals.
- Intervention: per-frame rectification gating — drop frames that fail the contract (explicit dropped_frames evidence, not silent), require >=75% kept fraction for solver readiness. BundleSDF tolerates missing frames (it tracks across gaps).
- Prediction: tomato/pink-lid/off-white/black-bag jobs become solver-ready with <10% dropped; white bag may stay below threshold (its outliers cluster where the bag leaves frame).

2026-06-12 01:50 Predictions for full-interval BundleSDF (before results). Rigid objects (faucet 62f, pink-lid 211f, off-white can 336f): expect complete ob_in_cam pose sequences and meshes passing rasterized front-surface depth QC (short pink-lid segments passed at median 1.9-3.4mm). Deformable objects (black bag 348f, white bag 356f, peel 217f): expect pose tracking to survive but mesh depth QC to fail or degrade — that outcome would be evidence that deformation state is physically required (consistent with docs: bags need reference surface + deformation, not a rigid mesh), not a solver bug. Tomato (649f): near-rigid, expect pass but runtime is the risk; failure mode to watch is tracking drift across the 0274-0939 interval with hand occlusion. Falsification measure: per-job evaluator status in v17_geometry_reconstruction_results_full_interval.

2026-06-12 01:45 Deliverable assembly chain traced end-to-end (reading, no execution):
1. V16 annotations_v16_full.json: full-length per-frame camera T_world_camera_metric, hands, and real per-frame captions (from egoscale actions JSON, already baked in). Caption source confirmed live.
2. V17 hand state: interior-owned graph rows carry final ray shifts + pose deltas per (frame,side); a new artifact must bake them into annotation hands (replace_anchor_hands pattern in build_v17_full_state).
3. Object stream: BundleSDF ob_in_cam (per rectified-job index) + canonical mesh -> per-frame world meshes via T_world_camera @ ob_in_cam (adapt_bundlesdf_to_mesh_archive_v3 pattern). Multi-object support: save_mesh_archive stores one mesh per frame; multiple objects can be concatenated per frame (vertex stack + face offset) without renderer changes.
4. build_v17_full_state merges annotations + mesh archive + captions; render_v17_full_state -> fuse_v1_full_fidelity -> overlay/world/side-by-side full-duration.
Gap list for a real V17 deliverable: (a) accepted full-interval meshes (running), (b) multi-object world mesh archive builder consuming reconstruction results + job manifests + V16 camera, (c) hand-state baking from interior-owned graph, (d) full-state/render run with updated inputs, (e) honesty contract: keep QC banner unless quality review passes.

2026-06-12 02:10 First full-interval BundleSDF results vs predictions:
- PEEL (deformable, 217f contiguous): ACCEPTED, IoU median 0.84. Falsifies my prediction that deformables fail mesh QC — the peel is rigid enough over this interval. Positive surprise.
- FAUCET (rigid, 62f): REJECTED, IoU median 0.27 with zero-IoU frames. Mechanism is not rigidity: the exported "interval" is two disjoint active segments (342-369, 906-939) with a 537-frame hole; BundleSDF tracking cannot jump it. Prediction (a) wrong for the right-discoverable reason: job granularity must be per contiguous active segment, not per object.
- Affected pending jobs: white bag (gap 444 frames between segment 0-90 and 534+), faucet. Black bag max gap 12f, pink-lid/off-white/tomato/peel contiguous — unaffected.
- Intervention: split exporter jobs at exported-frame gaps > 30; re-export; update remote launcher before queue reaches trash jobs (tomato still running, so there is time).
- Prediction: faucet segment 906-939 (34f) and 342-369 (28f) pass individually if 20+ frames suffice; white bag segments still risk deformation failure but no longer the gap artifact.

2026-06-12 03:00 Pass-1/2 BundleSDF status integration. Tomato (649f) finished with mesh_cleaned.obj — all three tomato-case contiguous jobs now have backend outputs (faucet-merged is stale/replaced by split jobs queued in pass 2). Pass-2 queue started trash pink-lid job on GPU 2. Open risk inventory for the deliverable: (1) tomato mesh must pass evaluator projection/depth QC — unknown until post-jobs chain runs; the short-segment history says keyframe-mode meshes can pass; (2) deformable bags (black/white) may fail QC -> the archive will then carry can+peel+tomato+faucet subset; that is still an honest partial object stream, and bag deformation becomes the falsified-rigid evidence for the next version's deformation state design; (3) render QC validates duration only — visual review of hand/object alignment is the decisive deliverable gate and stays with the user/me after chain completion.

2026-06-12 03:20 Camera-model ownership in baked annotations (found by tracing, before render review). Baked hand depths are ~0.353x the V16 depths (1,083 rows, IQR 0.34-0.364) because the interior-owned solver works in UniDepth-scaled intrinsics (fx~1090 at 1920px) while V16 annotations carry nominal fx=2304. Without intrinsics correction the overlay projection of baked hands would be wrong. Fix: baking now overwrites hand source_intrinsics with the solver camera (prior preserved as v16_source_intrinsics, camera_model labeled). Scale topology: corrected hands + BundleSDF objects share the UniDepth metric camera; V16 T_world_camera trajectory was scaled by V16 hand-depth/DROID ratios, so per-frame hand-object relations are consistent but inter-frame world motion magnitude may be biased. This is the camera/depth consistency item of the open V3 joint solver; record, do not patch silently.

2026-06-12 04:15 V18 design inputs accumulating from V17 evidence (no design doc yet; per AGENTS.md a new version requires an upfront design before implementation):
1. Camera/metric unification is the deepest unresolved mechanism: three camera models coexist (V16 nominal fx=2304 + DROID-scaled world, UniDepth per-frame intrinsics, BundleSDF rectified constant-K). V17 bridges them with per-layer adapters; V18's core should be one joint metric state (the original V3 joint solver intent) with UniDepth-scaled intrinsics as the primary camera and the world trajectory re-scaled accordingly.
2. Deformation state: bags (and possibly peel at longer horizons) need reference-surface + deformation variables; BundleSDF rigid meshes are the canonical-geometry source but per-frame pose alone cannot close bags if QC rejects them.
3. Contact ownership becomes tractable once hands and objects live in the same metric camera (already true post-baking): pairwise hand-vertex/object-mesh distances can now be computed against reconstructed meshes instead of visible surfaces — the missing physical contact factor V17 has chased.
4. Acceptance predicates must be visibility-valid by construction (edge-band lesson), not patched after the fact.

2026-06-12 04:45 Visual projection QC (frame 780, tomato case): (1) baked hands with solver intrinsics project onto the actual hands washing the tomato (green-dot overlay) — intrinsics fix verified visually, not just numerically; (2) peel world mesh projects precisely onto the black tray at image top where pinkish peel material is visible in RGB — the reconstructed object is in the right world location through the V16 camera transform. Limits: single frame, dot-cloud projection (not raster), no contact-frame test. Saved: /tmp/v17_handbake_projection_qc_780.jpg, /tmp/v17_peel_projection_qc_780_rectK.jpg.

2026-06-12 05:00 Hand-object world-distance QC (peel, frames 700-900): min hand-peel distances stable 21-29cm. Consistent with scene semantics: the tracked peel object is the discarded peel pile on the tray; hands work at the sink. No metric contradiction between baked hands and reconstructed object in shared world frame. Known visual-review items for the deliverable: legacy V16 single-object mask overlay still draws alongside the new mesh overlay (may look redundant); world-view scale bias from V16 DROID-scaled trajectory vs UniDepth metric (recorded 03:20) shows up as possible camera-motion magnitude error in the 3D view.

2026-06-12 09:27 Live status after rehydration. Observation: remote full-interval BundleSDF pass-2 produced mesh_cleaned.obj for every current contiguous job, including trash can/bag segments, tomato, peel, and split faucet segments. Observation: the authoritative post-jobs evaluator is still running all-face QC, while an older summary on disk is stale and should not be interpreted as final evidence. Observation: duplicate evaluator processes were writing the same result root; stopping the standalone process preserves single-writer evidence without changing the measurement contract.
- Interpretation: the next discriminating evidence remains the all-face reconstruction-result report, not a new diagnostic. The long runtime is expected under the current all-face raster contract because docs record that capped-face QC created false depth tails; relaxing the evaluator would weaken the reconstruction acceptance claim.
- Live prediction: if the chain evaluator completes normally, accepted objects become the only objects included in the multi-object world archive and final partial renders; rejected objects become evidence for rigid/deformation or tracking failure, not closure blockers to be hidden. If the evaluator fails or stalls beyond the current last-segment runtime envelope, the mechanism to inspect is evaluator scalability/progress reporting, not object geometry itself.
- Scope: readiness flags remain false until full-interval object geometry, contact-compatible ownership, and visual review close; current work is pipeline movement toward an honest partial V17 deliverable, not V17 closure.

2026-06-12 10:09 V17 final failure commitment.
- Claim: V17 failed as a pipeline design. Evidence: runtime grew to tens of hours for roughly one-minute clips, the central object path used BundleSDF/test-time neural rigid reconstruction, occlusion remained missing-data/diagnostic handling rather than explicit state estimation, HaWoR infill was not validated, and no acceptable full-video annotation deliverable was produced.
- Preserved useful mechanisms: interior-owned hand/depth measurement and depth-edge visibility invariant; model-produced object plans and masks; contact-owner contradiction diagnostics; visible-surface/material-track evidence.
- Decision: stop V17 development and move to V18. V18 must test a bounded-cost occlusion-aware design first; more V17 measurement audits would not reduce the dominant uncertainty.
