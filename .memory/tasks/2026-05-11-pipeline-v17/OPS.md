# OPS ledger — pipeline v17 (append-only)

2026-06-11 23:18 Rehydrated v17 execution from .memory/tasks/2026-05-11-pipeline-v17/PROMPT.md. Read repo PROMPT.md, AGENTS.md, LOG.md tail, git status/log. Working tree clean except untracked scripts/build_v17_full_residual_depth_owner_diagnostic.py (667 lines, draft, not established evidence). HEAD bd14b6c "Add V17 full residual surface-tail diagnostic". Created OPS.md/EPISTEMIC.md ledgers (task dir previously had only PROMPT.md). Next: review docs/pipeline_v17.md + pose-enabled full-residual solver, judge the untracked draft against the PROMPT.md yes/no discrimination question, then build the depth-ownership solver iteration.

2026-06-11 23:35 Verified and executed untracked scripts/build_v17_full_residual_depth_owner_diagnostic.py (.venv py_compile OK, pyright 0 errors, run 4.3s, exit 0). Outputs under /data2/ego_annotation_outputs/v17_full_residual_depth_owner_diagnostic/{summary,trash_1050,task5_tomato_960}. All FALSE_READY flags preserved. Decision per PROMPT.md gate: the diagnostic DOES discriminate depth-owner classes -> track it, wire into joint contract, cite as evidence.

2026-06-11 23:58 Built and ran scripts/build_v17_depth_edge_ownership_counterfactual.py (py_compile OK, pyright 0 errors, 52s, exit 0). Output /data2/ego_annotation_outputs/v17_depth_edge_ownership_counterfactual/. Hand-independent UniDepth depth-discontinuity band: 7px window, 0.10m local range, 6px dilation; acceptance recomputed on interior owner pixels of the unchanged pose-enabled solution.

2026-06-12 00:20 Built scripts/solve_v17_interior_owned_full_residual_hand_graph.py (py_compile OK, pyright 0 errors). First launch failed: source_summary required method/status strings but annotations report lacks them; relaxed to optional. Running in tmux ego_annotation:interior_graph (local 3080, ~31% util). trash_1050 complete at 00:13 (~10min); task5_tomato_960 running. Output root /data2/ego_annotation_outputs/v17_interior_owned_full_residual_hand_graph/.

2026-06-12 00:35 Interior-owned full-residual solver completed both cases (tomato finished ~00:30; first tmux EXIT=1 was the source_summary strictness bug, rerun EXIT=0). Wired interior_owned_full_residual_hand_graph into build_v17_joint_solver_problem.py (CaseInputs field, loader, counts fn, families exposure, cross-checks vs pose graph variable/accepted rows, per-case + global summary keys, CLI root). py_compile + pyright clean. Joint contract regeneration running in tmux ego_annotation:interior_graph.

2026-06-12 00:48 Committed 3ff2121 (interior-owned solver + joint contract wiring + docs). Joint regeneration JOINT_EXIT=0; summary and per-case audits passed; all readiness flags false. LOG.md updated. tmux window interior_graph closed. Commits this session: bd72f09 (depth-owner diagnostic), 2cf1827 (edge counterfactual), 7bdd346 (.gitignore .memory/), 3ff2121 (interior-owned graph).

2026-06-12 00:55 Object-side execution resumed after user correction (task = deliverable, not hand-side report). Built scripts/build_v17_full_interval_geometry_reconstruction_jobs.py (py_compile + pyright clean): full-active-interval BundleSDF jobs per exported object-track dataset, same rectification contract as seed-window jobs. Export running in tmux ego_annotation:fullint_jobs (~553MB per 440-frame job). Remote situation: A800 GPUs 0/2/3 free; /mnt/user-home 100% full (3.7G), root 98%; /mnt/truenas-user-home writable with 36T free -> created /mnt/truenas-user-home/yiwen/v17_full_interval for inputs/outputs. Queue plan: remote_queue_v17_bundlesdf_jobs.sh ROOT=/mnt/user-home/yiwen/ego_annotation_remote INPUT/OUTPUT on truenas, keyframe mode, depth_weight 0 (matches the accepted short-segment configuration). Note: queue serializes runs via flock on shared BundleSDF config.

2026-06-12 01:20 Per-frame rectification gating added to full-interval job exporter (drop bad frames explicitly, min_kept_frame_fraction=0.75). Pass 2: all 7 jobs READY — black bag 348/437 kept (0.80), off-white can 336 (0.97), pink-lid 211 (0.91), white bag 356 (0.82), faucet 62 (1.0), tomato 649 (0.99), peel 217 (1.0). Prediction confirmed: failures were per-frame intrinsics outliers, not systematic. rsync of 3.8G inputs to A800 truenas (/mnt/truenas-user-home/yiwen/v17_full_interval/inputs/) running in tmux ego_annotation:sync. Queue/runner scripts synced to remote. User rule added to PROMPT.md constraints: never block on sleep; arm follow-ups in tmux and do parallel work.

2026-06-12 01:35 Remote queue first launch failed twice: (1) rsync of runner scripts dropped executable bit (local remote_run_bundlesdf_v3.sh is -rw); fixed with chmod +x on remote. (2) I manually relaunched before the 3.8G input sync finished -> missing rgb dir error. Re-armed queue launch behind SYNC_DONE_OK gate inside tmux ego_annotation:remote_queue. Remote session: v17_fullint_bundlesdf on A800 (GPUs 0/2/3 free). Queue order: pink-lid, off-white can, black bag, white bag, then faucet, peel, tomato (649f last; runtime risk). Outputs: /mnt/truenas-user-home/yiwen/v17_full_interval/outputs/<case>/<job_id>.

2026-06-12 01:05 Remote queue dispatched prematurely: the tmux gate grep matched its own command text containing SYNC_DONE_OK, so trash jobs launched before rsync finished (JOB_FAILED missing rgb x4). Faucet job started successfully on GPU 2 (its input had synced). Fix: re-armed gate on pgrep of the live rsync process; after verified sync it re-rsyncs (idempotent) and redispatches the launcher into the remote pane, which executes after the current launcher exits; completed jobs skip via mesh_cleaned.obj check. Lesson: never gate on pane text that the gate command itself prints.

2026-06-12 01:50 Pipeline plumbing for full-interval results: --reports-only mode rebuilds case reports from per-job manifests; evaluator-compatible summary/report aliases added (commit 0c97331). Dry-run of build_v17_geometry_reconstruction_results against the full-interval roots passes: 7 jobs, 6 pending, 1 output detected (faucet running on A800 GPU 2, NeRF stage). Armed tmux windows: remote_queue (verified-sync redispatch of remaining 6 jobs), outsync (10-min polling rsync of outputs back to /data2/ego_annotation_outputs/v17_bundlesdf_outputs_full_interval). Next decision point: faucet mesh QC result -> if accepted, the same path scales to all 7; if rejected, compare against accepted short-segment configuration before tuning.

2026-06-12 01:25 BundleSDF full-interval progress: faucet (62f) done 8min, peel (217f) done 3.5min with mesh_cleaned.obj 5.4MB and 217/217 ob_in_cam poses but only 4 keyframes selected (small-object/feature-poor warning: "total keyframes=4, want to select 10"), tomato (649f) running on GPU 2. Trash inputs sync completed (REDISPATCH_SENT_AFTER_VERIFIED_SYNC); trash jobs queue after tomato. Evaluator EVAL3 running in tmux ego_annotation:evaluator with synced outputs; previous EVAL2/EVAL3 confusion was a stale-sync race (report written 01:07, mesh synced 01:08) — evaluation result is only valid if report mtime > last outsync mtime.
2026-06-12 01:30 Deliverable-chain recon (parallel work): adapt_bundlesdf_to_mesh_archive_v3.py converts BundleSDF ob_in_cam + canonical mesh + camera T_world_camera_metric into per-frame world mesh archives (the format the renderer consumes). Semantic captions: raw egoscale task JSON has per-action segments with descriptions (actions[].start_frame/end_frame/action/description) — caption source exists; build_v17_full_state.annotate_v17_captions consumes per-frame captions. Render path: build_v17_full_state.py + render_v17_full_state.py produce full-duration overlay/world/side-by-side with QC banner.

2026-06-12 01:50 Deliverable chain artifacts built in parallel with remote BundleSDF: (1) build_v17_multi_object_world_mesh_archive.py (commit c248030) — accepted reconstructions -> per-frame multi-object world mesh archive via T_world_camera @ ob_in_cam, concatenated per frame with face offsets; consumes evaluator acceptance only. (2) build_v17_interior_owned_hand_annotations.py (commit 98e8620) — bakes interior-owned graph hand state (pose delta + case scale + ray shift via replay_vertices corrected output, same path as solver eval) into annotations with per-hand provenance labels; running in tmux ego_annotation:handbake. Evaluator EVAL3 still running (~20min; 649-frame tomato job rasterization is heavy). Remote: tomato BundleSDF in NeRF final stage. Render chain plan: baked annotations + multi-object archive -> build_v17_full_state-style merge -> fuse_v1_full_fidelity render-only -> full-duration overlay/world/side-by-side.

2026-06-12 02:00 Hand baking complete (HANDBAKE_EXIT=0): trash 843 baked + 1,065 kept-prior, tomato 1,083 baked + 776 kept-prior; wrist-to-middle-tip medians 11.6cm/13.5cm (physically plausible). Annotation outputs under /data2/ego_annotation_outputs/v17_interior_owned_hand_annotations/<case>/annotations_v17_interior_owned_hands.json with per-hand provenance. Added build_v17_deliverable_state.py (commit 0588ba9) — assembles render manifests from baked annotations + multi-object archive with frame-count contract checks; refuses empty object stream. EVAL3 still rasterizing (peel mesh ~100k faces x 217 frames). Remaining chain once evaluator + tomato + trash jobs finish: archive builder -> deliverable state -> fuse_v1 render-only -> full-duration videos.

2026-06-12 02:20 Segment-split re-export done (SPLIT_EXIT=0): 9 jobs all ready. White bag -> 000015_000090 (60f) + 000534_000912 (334f); faucet -> 000342_000369 (28f) + 000906_000939 (34f); others unchanged. Committed 9db4da1 (gap splitting + --only-tracks). Pass-2 chain armed: sync window waits for reports rebuild then rsync --delete inputs; remote_queue window waits for SYNC2_VERIFIED_DONE (non-self-matching grep pattern) then dispatches v17_fullint_launch2.sh, which queues in the remote pane behind the running tomato job. Stale merged-job outputs (old faucet 342_939) are ignored by the evaluator since job ids come from the current jobs report.

2026-06-12 02:40 Pass-2 fully armed and verified: SYNC2_VERIFIED_DONE (rsync --delete, split jobs on remote), PASS2_DISPATCHED (launcher2 queued in remote pane behind tomato run). Tomato BundleSDF in final NeRF stage (keyframes#: 201, last_stamp 000648 = all 649 frames tracked). Post-jobs chain armed in tmux ego_annotation:outsync (poll-sync -> evaluator -> mesh archive -> deliverable state; copy preserved at .memory/local/v17_post_jobs_chain.sh). Added render_v17_deliverable_state.py (commit c363625): full-duration render driver consuming deliverable-state manifests, honest banner ("V17 PARTIAL: interior-owned hands + reconstructed-object subset"), hard duration QC, readiness flags false.

2026-06-12 02:55 Smoke test of deliverable chain on tomato case (peel-only object stream): archive builder fixed (mkdir before savez), archive built — 217 frames, peel extents ~27cm stable across frames (rigid-pose sanity pass), coverage 0.226 of annotation timeline. Deliverable state + full-duration render (960 frames) running in tmux ego_annotation:smoketest. This is a chain validation, not the final render: tomato/trash objects will be added by the armed post-jobs chain when BundleSDF pass-2 completes.

2026-06-12 03:30 Status snapshot. Remote A800 pass-2: pink-lid full job done (BUNDLESDF_RUN_V3_OK), off-white can running; remaining queue: black bag, white bag x2, faucet x2. Local: EVAL4 (tomato mesh QC) running; smoke render (peel-only tomato case) running — will be superseded by rebake; HANDBAKE2 rebake with solver-camera intrinsics fix (a659a8c) running. The smoke render uses pre-fix annotations, so its overlay hand projection is known-wrong; keep it only as a renderer-path validation, do not review its visual quality. After rebake completes, deliverable state + render must be regenerated from the fixed annotations. Post-jobs chain in outsync window will rebuild archive/state automatically after pass-2; the final render needs a manual (or newly armed) run after both rebake and chain complete.

2026-06-12 03:45 Final assembly armed in tmux ego_annotation:final (copy at .memory/local/v17_final_assembly.sh): waits for rebake -> post-jobs chain (CHAIN_ALL_DONE) -> smoke render exit, then rebuilds deliverable state from rebaked annotations + final multi-object archive and renders both cases full-duration. Full automation graph now: remote pass-2 queue -> outsync chain (sync->eval->archive->state) -> final window (state->render). Remaining manual gates after FINAL_ALL_DONE: joint-contract regeneration with new object/hand counts, LOG/docs updates, commit, and visual quality review of the three videos per case.

2026-06-12 04:00 Joint contract now consumes full-interval reconstruction results as a parallel input (commit 8f141d6): per-case keys full_interval_geometry_reconstruction_{job,pending,accepted_result}_count + status_counts; status_counts also added to the shared counts fn (verified present in old reports). Remote pass-2: pink-lid done, off-white done (11min), black bag running on GPU 1. Three local jobs still running: EVAL4 (tomato QC, 20min in), smoke render (25min in, writing side_by_side), HANDBAKE2 rebake. All downstream steps armed: outsync chain + final assembly window.

2026-06-12 04:30 Rebake HANDBAKE2_EXIT=0: 1,926 baked + 1,841 kept-prior across both cases; intrinsics fix verified (frame 0 trash: fx baked 716.9 vs v16 2304, camera_model=unidepth_scaled_source_intrinsics). Final assembly window will pick up these annotations. Remote: 3 trash jobs done (pink-lid, off-white, black running->? recheck), tomato evaluator at 25min (likely rasterizing 649-frame tomato mesh).

2026-06-12 05:15 Runtime notes: EVAL4 at 30min — tomato job is 649 frames x 207k-face mesh all-face rasterization; expected heavy, not hung (CPU ~120%). Smoke render at 35min for 960 frames (renderer is CPU-bound at ~1.6 fps with mesh edge drawing; full final render of both cases will take ~1.5-2h — acceptable inside the armed final window). Black bag BundleSDF in pose-graph stage. Visual QCs saved and logged (hand projection + peel projection + hand-object distances): no metric contradictions found pre-render.

2026-06-12 05:45 Evaluator timeline disambiguated: tmux send-keys queue meant EVAL4 only started when EVAL3 exited (02:08); the on-disk reports (tomato rejected_missing_solver_mesh) are STALE EVAL3 output from before the tomato mesh sync. EVAL4 (43min in) is the live pass including tomato 649f x 207k faces; expected ~1.5-2h. Remote pass-2: black bag done (02:41), white bag 000015_000090 done (02:48), white bag 000534_000912 next, then faucet x2. The outsync chain will run its own final evaluator after ALL_JOBS_PASS2_DONE, superseding EVAL4; EVAL4 kept for early tomato QC signal.

2026-06-12 09:27 Continuation rehydration after user request. Read PROMPT.md, OPS.md, EPISTEMIC.md, AGENTS.md, LOG.md, docs/pipeline_v17.md, git status. Working tree clean. Live tmux graph: remote BundleSDF pass-2 complete (ALL_JOBS_PASS2_DONE at 03:26), post-jobs chain active in ego_annotation:outsync, final assembly waiting in ego_annotation:final. Local BundleSDF outputs synced for 9/9 current contiguous jobs with mesh_cleaned.obj present. Existing evaluator summary is stale (still reports pass-2 jobs pending). Two identical local all-face reconstruction evaluators were writing the same full-interval results root: chain-owned PID 4029294 and standalone evaluator PID 4042814. Sent SIGINT to standalone PID 4042814 to remove the writer race and preserve CPU for the chain; chain-owned evaluator continues. Chain evaluator has generated metric QC meshes for all synced jobs; last written file was the 334-frame white-bag segment metric mesh at 09:03 and the process remains CPU-bound, consistent with all-face projection/depth QC on the final long segment. No source edits made.

2026-06-12 10:09 V17 formally closed as FAILED per user instruction. Active local V17 tmux automation/evaluator/final-render windows were stopped; no further V17 compute should be launched except to inspect preserved evidence. Canonical failure/design transition recorded in docs/pipeline_v17.md, docs/pipeline_v18.md, AGENTS.md, and LOG.md. V18 task memory initialized at .memory/tasks/2026-06-12-pipeline-v18/.
