[project]
name = "ego-annotation"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
  "chumpy>=0.70",
  "mediapipe==0.10.14",
  "numpy>=1.26,<2",
  "opencv-python-headless>=4.9",
  "torch>=2.0",
]

[tool.uv]
managed = true

[tool.uv.extra-build-dependencies]
chumpy = ["pip"]
